# v14 Review 全面修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 ChatGPT_v14.md 审核结论，分三批修复全部 P0/P1 问题，使「下次正式训练前必须完成」的第一批全部落地，并推进第二批（Lockbox 前）与第三批（工程优化）。

**Architecture:** 第一批聚焦研究语义正确性（Requested Universe 对账、Gate 日历 artifact、Topic Formal、`--all` 内存上限）；第二批聚焦 PIT/身份/记账闭环（Provider 单位校验、Storage Formal read、pct_change 重算、Baseline recipe 身份、tape weight hash、vintage 语义、原子写入、Dataset 重构、文档措辞）；第三批为工程拆分与清理。Phase 0 先把工作区中 in-flight 的 §十一/§十二 改动测试补绿，作为全部后续任务的可信地基。

**Tech Stack:** Python 3.12, pandas/numpy, PyTorch, OmegaConf, pytest。venv: `PYTHONPATH=. ./.venv/Scripts/python`。

**参考文档:** `ChatGPT_v14.md`（审核全文，每条任务标注对应章节）；`CLAUDE.md`（工作流：每个 Task fan out subagent + 双阶段审查）。

---

## Phase 0 — 地基补绿（in-flight 改动调和）

### Task 1: 调和 §十一/§十二 相关测试，使 `pytest tests/` 全绿

**Files:**
- Modify: `tests/data/test_download_resume.py`（6 个失败用例）
- Modify: `tests/data/test_contract.py::TestPctChange::test_first_row_nan_allowed`（1 个失败用例）
- Read first: `stoke_ml/data/download_resume.py`、`stoke_ml/data/contract.py`（工作区已改，未提交）

**背景（必须先读）:** 工作区已实现 v14 §十一 —— `write_stock_manifest` / `mark_stock_result` 删除 `covers_request` 布尔 override 参数，新增 `effective_start/effective_end` 与派生结论 `effective_range_covered`；§十二 —— `validate_pct_change` 新增算术恒等式检查、`validate_price_volume_amount_consistency` 新增 `volume==0&amount>0` / `volume>0&amount==0` 硬拒绝。旧测试仍调用 `covers_request=`（现为 TypeError）或使用算术不自洽的 pct_change 数据。

- [ ] **Step 1: 读工作区已改代码，理解新语义**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/data/test_download_resume.py tests/data/test_contract.py::TestPctChange -q 2>&1 | tail -20`
Expected: 列出 6 个 download_resume 失败（`TypeError: unexpected keyword argument 'covers_request'`）+ 1 个 contract 失败（`pct_change_inconsistent:2`）。

- [ ] **Step 2: 重写 6 个 download_resume 失败用例到新 §十一 模型**

新语义：
- 「上市晚于 requested_start 时 caller 断言覆盖」的旧表达 `covers_request=True` → 新表达 `effective_start=listing_date`（window 被裁剪到股票生命周期后 `actual covers effective` 成立）。
- 「covers_request=False 强制重下」→ 表达为 effective window 未覆盖（不传 effective 时即 requested window 未覆盖），断言 `effective_range_covered` 为 False 且 `_manifest_matches` 返回 False。
- 「stored covers true 但日期撒谎不跳过」→ 断言 legacy `covers_request:true` 但 `effective_range_covered` 派生为 false 时被拒绝；新 manifest 的 `covers_request` 是派生的（镜像 effective_range_covered）。
- `TestSkipCompletedYears` 的失败同理：year manifest 不再接受 `covers_request=` 参数。

用 `_manifest_matches` 的既有断言风格改写（读取 manifest JSON 断言 `effective_range_covered` / `effective_start` / `effective_end` 字段）。**删除**所有仍传 `covers_request=` 的调用。

- [ ] **Step 3: 修 contract 失败用例**

`_daily(pct_change=[np.nan, 2.0, 3.0])` 的 close 序列未与 pct_change 对齐。把测试数据改成算术自洽：例如 `close=[100, 102, 105.06]`、`pct_change=[np.nan, 2.0, 3.0]`（`100*(102/100-1)=2.0`、`100*(105.06/102-1)=3.0`）。若 `_daily` helper 自动从 close 派生 pct_change，则检查其派生逻辑；若需显式传 close，确保两者一致。同时补一个断言：算术不一致的 pct_change 现在会被拒（`pct_change_inconsistent`）。

- [ ] **Step 4: 全量快速冒烟确认绿**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/ -q 2>&1 | tail -5`
Expected: `passed`，无 FAILED。

- [ ] **Step 5: Commit**

```bash
git add tests/data/test_download_resume.py tests/data/test_contract.py
git commit -m "fix: reconcile §十一/§十二 tests with effective-range coverage model"
```

---

## Phase 1 — 第一批（下次正式训练前必须完成）

### Task 2: §八-2 Formal Gate 强制 Requested Universe 对账

**Files:**
- Modify: `scripts/production/data_quality_gate.py`（`--requested-universe` / `--request-manifest` 已支持）
- Modify: `scripts/production/build_features.py`（`--quality-gate` 默认未传下载 Run Manifest）
- Modify: `scripts/production/train_panel.py`（训练入口要求报告含 Universe reconciliation）
- Test: `tests/scripts/test_train_panel_universe.py`、`tests/data/test_data_quality_gate.py`

**要求（v14 §八-2）:** CSI 成员 ∩ 磁盘实际存在的解析会静默删掉缺失股票（数据可用性偏差/退市缺失/下载失败消失），Gate 仍显示「现有文件的 98% 有效」。正式训练必须要求：下载 Run Manifest → Requested universe 对账 → 每只股票文件/Manifest/范围状态 → Quality Gate → 训练。不能让「磁盘上有什么就研究什么」成为正式 Universe 定义。

- [ ] **Step 1:** 读 `data_quality_gate.py` 中 `_build_universe_request`、universe check、报告 schema；读 `build_features.py` 与 `train_panel.py` 的 `--quality-gate` 接入点。
- [ ] **Step 2:** `build_features.py --quality-gate` 把下载 Run Manifest 路径 + requested universe 传给 gate；缺 Run Manifest 时 Formal 模式失败（而非静默按磁盘现状解析）。
- [ ] **Step 3:** gate 报告新增 universe reconciliation 结果：requested ∩ available / missing / 每只股票 manifest 状态；`train_panel.py` 正式入口要求该检查存在且 missing 处理策略明确（显式列出而非静默删除）。
- [ ] **Step 4:** 补测试：构造带缺失股票的 requested universe，断言 Formal 报告反映 missing 而非假装 98% 有效。
- [ ] **Step 5:** 跑 `pytest tests/scripts/test_train_panel_universe.py tests/data/test_data_quality_gate.py tests/ -q` 全绿。
- [ ] **Step 6: Commit**

### Task 3: §九 Gate 用 data_dir 冻结 Calendar artifact + freshness 修正

**Files:**
- Modify: `scripts/production/data_quality_gate.py`
- Read: `stoke_ml/data/calendar.py`（`get_research_calendar`、artifact 哈希）

**要求（v14 §九）:** `_CALENDAR` 模块导入时创建，未按 `--data-dir` 加载对应 Calendar artifact；报告只记录 CALENDAR_VERSION 而非 artifact 内容哈希；freshness 用 `today - latest_date > 4 自然日` 判断，春节/国庆长假误报。

- [ ] **Step 1:** 定位 gate 中 `_CALENDAR` 定义与 freshness 判定逻辑。
- [ ] **Step 2:** 改为按 `--data-dir` 加载冻结 Calendar artifact（`get_research_calendar` 族），报告记录 artifact 内容哈希（非版本号）；artifact 缺失时 Formal 失败而非退回代码规则。
- [ ] **Step 3:** freshness 改为 `latest_date == 最近一个理论上已发布完成的正式交易日`（用冻结日历取最后一个已完成交易日），去掉自然日差值。
- [ ] **Step 4:** 补测试：长假场景下最新数据不误报过期；不同 data-dir 的 calendar artifact 哈希不同。
- [ ] **Step 5:** 跑相关测试全绿。
- [ ] **Step 6: Commit**

### Task 4: §十 Topic Formal 安全

**Files:**
- Modify: `stoke_ml/preprocessing/text/topics.py`
- Modify: `scripts/production/fit_topic_model.py`
- Test: `tests/preprocessing/text/test_topics.py`、`tests/scripts/test_fit_topic_manifest.py`

**要求（v14 §十 2/3/4）:**
1. `_collect_silver()` 只有在 `aligned_date` 存在时才 cutoff 过滤 → 改为必需列，缺失立即终止。
2. 单股票加载失败被 warning 跳过 → fit manifest 记录 `requested_stocks/loaded_stocks/failed_stocks/coverage threshold/status=DEGRADED`。
3. `TopicModeler.transform()` 捕获异常变 `topic_id=-1` → 新增 `transform(..., formal=True)`，Formal 模式异常直接终止。

- [ ] **Step 1:** 读 topics.py 的 `_collect_silver`、fit、transform；读 fit_topic_model.py 的 manifest 写入。
- [ ] **Step 2:** `aligned_date` 列为必需（缺失即 Formal fail，配测试）。
- [ ] **Step 3:** fit manifest 增加 coverage 字段与 `status=DEGRADED` 语义；低于覆盖率阈值的 fit 进入 DEGRADED 并影响下游。
- [ ] **Step 4:** `transform(..., formal=True)` 严格模式。
- [ ] **Step 5:** 跑相关测试全绿。
- [ ] **Step 6: Commit**

### Task 5: §七 明确限制 `--all`，避免 96GB OOM

**Files:**
- Modify: `scripts/production/train_panel.py`（`--universe all` / `csi800` 守卫）
- Modify: `stoke_ml/models/panel/config.py` 或训练入口 config
- Modify: docs（`README.md` / `CONTEXT.md` 中 Panel 维度与可运行性描述）
- Test: `tests/scripts/test_train_panel_universe.py`

**要求（v14 §七）:** 全市场 5530 只特征数组约 225GiB，96GB 机器必然 OOM；`csi800` 历史成员并集已接近风险区。默认 500 大概率可跑但峰值可达数十 GB。需在 CLI/config 层明确限制并给出可读错误，同时文档诚实说明上限。

- [ ] **Step 1:** 读 train_panel.py 的 universe 解析与内存估算逻辑。
- [ ] **Step 2:** `--universe all` 直接拒绝（明确错误信息：估计内存需求 vs 可用内存）；`csi800` 给警告或按可用内存预检。提供可覆写的 escape hatch（如 `--allow-high-risk-universe`）但默认安全。
- [ ] **Step 3:** 更新文档：`--all` 不可运行、`csi800` 风险、500 默认的安全说明。
- [ ] **Step 4:** 补测试断言 `--universe all` 被拒绝。
- [ ] **Step 5:** Commit

---

## Phase 2 — 第二批（Lockbox 前完成）

### Task 6: §十二 Provider Adapter 单位校验 + 真实 fixture

**Files:**
- Modify: `stoke_ml/data/sources/a_shares/tushare_source.py`、`baostock_source.py`、`akshare_source.py`、`efinance_source.py`
- Test: 新增 `tests/data/test_provider_units.py`

**要求（v14 §十二 后半）:** Contract 层硬拒绝已完成。Provider Adapter 必须用真实响应 fixture 检查：Tushare `vol` 手→股、`amount` 千元→元；Baostock 股/元；AKShare/Efinance 实际单位；空值与零成交语义。新增真实响应 fixture（人工记录的典型 API 返回）驱动转换逻辑测试。

- [ ] **Step 1:** 读四个 provider 的响应 → DataFrame 转换函数。
- [ ] **Step 2:** 为每个 provider 构造真实形状的响应 fixture（含单位、空值、零成交行），断言转换后 volume/amount 单位正确且符合 Contract。
- [ ] **Step 3:** 断言「未转换」的错误（vol 仍为手）会被 Contract 硬拒绝。
- [ ] **Step 4:** Commit

### Task 7: §十三-1 Storage Formal read 传 Manifest + 官方 Calendar

**Files:**
- Modify: `stoke_ml/data/storage.py`（`load_daily(require_valid_manifest=True)` 路径）
- Test: `tests/data/test_storage.py` 或相关测试

**要求（v14 §十三-1）:** `validate_contract(result, ..., formal=True)` 未传 Manifest 与官方交易日，Provenance 依赖 `DataFrame.attrs`（不同 parquet engine 行为不一致）。改为把刚验证过的 Manifest 直接传给 Contract。

- [ ] **Step 1:** 读 storage.py 的 load_daily 与 validate_contract 调用链。
- [ ] **Step 2:** 将已验证 Manifest（含 requested/effective/actual 范围、schema_hash）与官方 trading days 传入 Contract 的 provenance 校验。
- [ ] **Step 3:** 补测试：无 attrs 的 parquet 也能通过 provenance（不依赖 attrs）。
- [ ] **Step 4:** Commit

### Task 8: §十三-3 合并历史修正后重算相邻 pct_change

**Files:**
- Modify: `stoke_ml/data/storage.py`（merge 路径）
- Test: `tests/data/test_storage.py`

**要求（v14 §十三-3）:** 合并采用同日期新行覆盖旧行；修正 t 日 close 后 t+1 日旧 pct_change 仍基于修正前 close。Contract 现在校验算术恒等式，但 canonical 写入应接受前重算。改为每次合并后统一重算 pct_change 并在写入前验证。

- [ ] **Step 1:** 读 storage merge / save 路径。
- [ ] **Step 2:** 合并后统一重算 `pct_change = 100*(close[t]/close[t-1]-1)`，首行保留 NaN，写入前过 Contract。
- [ ] **Step 3:** 补测试：修正 t 日 close 后 t+1 日 pct_change 被重算。
- [ ] **Step 4:** Commit

### Task 9: §十七 Baseline input recipe 纳入模型身份

**Files:**
- Modify: `scripts/production/train_baselines_panel.py`
- Test: `tests/scripts/test_train_panel_universe.py` 或 baseline 相关测试

**要求（v14 §十七）:** tape 的 `model_config_hash` 主要来自模型名 + PanelConfig，未绑定 `--with-seq-features`、baseline 输入构造版本、`max_train_rows`、超参、Scaler/Input recipe。新增 `baseline_input_recipe_hash` / `baseline_hyperparameter_hash` / `scaler_hash` / `training_sample_policy_hash` 单独保存。

- [ ] **Step 1:** 读 train_baselines_panel.py 的 hash 构造。
- [ ] **Step 2:** 增加四个 hash 字段并纳入 tape；写测试断言带/不带 seq features 时 input recipe hash 不同。
- [ ] **Step 3:** Commit

### Task 10: §十八 Continuous tape Weight hash + 重复预测日期 fail + 措辞修正

**Files:**
- Modify: `scripts/production/train_panel.py`（tape 写入）与 Continuous OOS 消费端
- Test: 相关测试

**要求（v14 §十八 1/2/3）:** 1) Formal tape 每 fold 必须含 `weight_hash` 且与对应 checkpoint 重算 SHA-256 一致；2) 同一股票同一信号日重复预测应失败（强不变量）；3) 报告措辞 "disjoint OOS return windows" → "disjoint signal windows"。

- [ ] **Step 1:** 读 tape schema 与 fold checkpoint 写入。
- [ ] **Step 2:** tape 增加 per-fold weight_hash（与 checkpoint 对账）。
- [ ] **Step 3:** 消费端拒绝重复预测日期。
- [ ] **Step 4:** 修正措辞（代码 Summary + 文档）。
- [ ] **Step 5:** Commit

### Task 11: §十五 Macro/Fundamental revision/vintage 语义

**Files:**
- Modify: `stoke_ml/data/fundamental_storage.py`、macro 存储、`stoke_ml/features/` lineage
- 正式报告标明哪些 Channel 是真正 vintage-safe，哪些只是「按发布日期对齐的最新修订历史」

**要求（v14 §十五）:** 即使修复真实 disclose_date，还要区分首次公布值与后来修订值；缺失 `vintage_time/revision_time/retrieved_at/version_id` 体系。最低限度：正式报告标注各 Channel 的 vintage 状态。

- [ ] **Step 1:** 读 fundamental/macro 存储与 feature lineage 报告。
- [ ] **Step 2:** 增加 retrieved_at / version_id 记录；报告新增 vintage 标注（vintage-safe vs latest-revised-aligned）。
- [ ] **Step 3:** 补测试断言报告含 vintage 标注。
- [ ] **Step 4:** Commit

### Task 12: §十三-2 辅助数据统一 Contract/Manifest/原子写入

**Files:**
- Modify: 各辅助数据 `scripts/production/*.py`（直接 `to_parquet()` 的）与对应 storage
- 引入 generation directory + current pointer 原子切换

**要求（v14 §十三-2）:** Parquet 与 Manifest 不是单一原子对象（replace parquet → replace manifest 之间崩溃 = 新 Parquet + 旧 Manifest）。更严格设计：generation directory + current pointer，让数据文件和 Manifest 作为一代整体切换。

- [ ] **Step 1:** 盘点直接 to_parquet 的辅助数据，选一个代表先做 generation 原子写入。
- [ ] **Step 2:** 实现 generation dir + pointer 切换；Formal read 拒绝 torn state。
- [ ] **Step 3:** 补测试：模拟两阶段崩溃，断言 non-formal 也读不到 torn state（或明确拒绝）。
- [ ] **Step 4:** Commit

### Task 13: §七/§十六 Dataset Date-centric / lazy storage 重构（大项，可拆子任务）

**Files:**
- Modify: `stoke_ml/models/panel/dataset.py`、`stoke_ml/models/panel/config.py`
- 特征网格改为 Memmap/Zarr / 分 chunk / lazy window gathering

**要求（v14 §七/§十六）:** 将 Dataset 改成以日期为一级样本（一个 batch = 一个日期 × 一组股票），Rank loss 获得完整横截面；每 epoch 主要遍历日期而非股票×日期；不构造数百万 Python index；可按日期 lazy 读取 Memmap/Zarr。特征网格用 Memmap/Zarr + lazy window gathering，而非一次性 (N,T,D) 内存数组。

> 这是最大的一项重构。建议拆为：① 日期一级索引与 batch 采样 ② Rank loss 横截面语义 ③ lazy 存储层（Memmap/Zarr）。逐个子任务可独立测试、独立 commit。

- [ ] **Step 1:** 读 dataset.py 的 `__getitem__`、`DateGroupedSampler`、batch 构造。
- [ ] **Step 2:** 设计日期一级索引结构（stock×date 稀疏 → 日期→stock 列表）。
- [ ] **Step 3:** 实现 lazy 存储（Memmap/Zarr）读写，特征按需加载。
- [ ] **Step 4:** 重写 sampler 为流式 yield（日期遍历）。
- [ ] **Step 5:** 补维度/形状/横截面语义测试，跑 panel smoke。
- [ ] **Step 6:** Commit（可多次）

---

## Phase 3 — 第三批（工程优化）

### Task 14: 拆分 FeaturePipeline/TrainPanel/Evaluate/Gate 超大文件

**Files:**
- Modify: `stoke_ml/features/pipeline.py`（~2000 行）、`scripts/production/train_panel.py`（~2900 行）、`scripts/production/data_quality_gate.py`（~1400 行）、`stoke_ml/features/aux_aligner.py`（~900 行）

**要求（v14 §二十一）:** 按职责拆成模块，保持公开 API 与 CLI 行为不变，测试全绿。拆完后再做 §十六 的 Date-centric 重构会轻松得多 —— **本任务应放在 Task 13 之前**（或与 Task 13 合并评估）。

- [ ] **Step 1:** 用 module 内职责边界（pipeline 拆：辅助合并/技术指标/序列构造/PK-PO 发现；gate 拆：check 定义/report/run）确定拆分目标。
- [ ] **Step 2:** 逐个拆分，保持 re-export 兼容。
- [ ] **Step 3:** 每拆一步跑对应测试。
- [ ] **Step 4:** 全量快速冒烟。
- [ ] **Step 5:** Commit

> **执行顺序提示:** Task 14（拆分）与 Task 13（重构）有先后依赖 —— 先拆再重构。执行到 Phase 2/3 交界时按此调整。

### Task 15: DateGroupedSampler 改为流式生成

**Files:** `stoke_ml/models/panel/dataset.py`（`DateGroupedSampler`）

**要求（v14 §七）:** `indices = []` 构造所有有效样本的 Python 整数列表 → 改为流式 `yield`，降低内存与 epoch 启动延迟。若 Task 13 已重写 sampler，本任务可能已包含在其中，则跳过。

### Task 16: 清理关键路径 broad except Exception

**Files:** Storage、Manifest、Feature build、Formal preprocessing、Experiment registry 相关模块

**要求（v14 §二十一）:** 数据源降级可宽容，但上述关键路径不应广泛吞掉编程错误。逐处改为精确异常；保留数据源降级的宽容捕获并加注释。

### Task 17: 真实 Provider 响应 fixture 测试

**Files:** 各 provider 测试目录

**要求（v14 §十二/§二十一）:** 用真实 API 响应形状的 fixture 驱动 provider 转换测试（与 Task 6 协同）。

### Task 18: 统一所有市场映射/股票代码/日历入口

**Files:** 仓库内所有 `TradingCalendar("a_shares")` 直调点、市场映射

**要求（v14 §九）:** 核心 Formal 路径已用 `get_research_calendar`，但 data_quality_gate、新闻/股吧下载、DataCenter、Fundamentals 下载、News Gold 构建、部分 Margin/龙虎榜/涨跌停/Sector 源仍直接 `TradingCalendar("a_shares")`。统一入口 + 市场路由单一函数（`market_of_code` 已建）。

### Task 19: 处理 Pandas GroupBy.apply 弃用警告

**Files:** `stoke_ml/preprocessing/text/aggregation.py`

**要求（v14 报告）:** 3 个 `GroupBy.apply` 弃用警告（Pandas 3.0 行为变更）。改为显式聚合写法（`include_groups=False` 或 vectorized 聚合）。

---

## 执行顺序总览

```
Phase 0  Task 1  (地基补绿)            ← 必须最先，所有任务依赖全绿基线
Phase 1  Task 2 → 5                     ← 第一批 P0，彼此独立，可并行 fan out
Phase 2  Task 6 → 13                     ← 第二批；Task 13 大项内部再拆
Phase 3  Task 14 → 19                     ← 第三批；Task 14 先于 Task 13 的话在 Phase 2 开头做
```

每 Task 完成后跑对应测试 + 快速冒烟 `pytest tests/ -q`（期望全绿无 FAILED），通过 spec 合规 + 代码质量两轮审查后标记完成。
