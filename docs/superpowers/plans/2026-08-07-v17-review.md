# v17 全面审核修复计划（三批）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

## Context

`ChatGPT_v17.md` 对 `Stoke_MachineLearning-master(10)` 版本做全仓静态审核 + 轻量动态验证。v17 的核心判断：**基础设施能力已经跑在生产入口前面**——Contract、Asset Manifest、Vintage、PanelStore chunk hash、Streaming builder 都写得越来越严格，但正式训练/下载入口还没真正强制使用。v17 §十九 列出 16 项优先级（P0 6 项 + P1 6 项 + P2 4 项）。

本轮按**全部 16 项**修复（用户已确认「全部 16 项」），commit 到 master。用户决策：§九 vintage 语义「两者都做」（safe-only 改名 revision-safe **且** 新增 headline-strict 档）；§十四 CSI 官方 effective-date membership **标记暂缓**（外部数据获取项，记 KB backlog）；v17 修完后系统到达冻结点，之后停止大规模重构，跑 Baseline → Ablation → Continuous OOS → Lockbox。

执行方式沿用 v15/v16 流程：`superpowers:subagent-driven-development`，每个 Task 由全新 implementer subagent → spec 合规审查 → 代码质量审查，两轮通过后提交。**串行执行，同一文件不并行**。计划落地时保存为 `docs/superpowers/plans/2026-08-07-v17-review.md`。

### 已核实的代码事实（直接读取当前 HEAD，非仅审计文档转述）

- **§五 formal 不强制 manifest**：`load_aux_data()`（`train_panel_panel.py:857-863`，签名 `(stock_list, data_dir, start_date, end_date, required_channels=None)`）内部实例化 NewsStorage(:885)/AnnouncementStorage(:893)/GubaStorage(:920)/CommentStorage(:928)/FundamentalStorage(:936)/MarketWideStorage×9(:949)/ETF(:956)/industry+market_env broadcast probe(:1008-1041)，**全部默认 `require_valid_manifest=False`**；全文件唯一 `require_valid_manifest=True` 在 `_resolve_panel` 的 K 线 daily(:295)。`load_aux_data` 无 formal 参数；`_formal_mode(args)` = `not args.no_formal`（`train_panel_gates.py:91-99`），已被 train_panel_panel.py:43 import 并在 :254 使用。
- **§六 live/prebuilt 路径分叉**：live `capital_flow` 读 `a_shares/capital_flow/`（`train_panel_panel.py:949` → `market_wide_storage.py:126`）；prebuilt fingerprint 读 `a_shares/capital_flow_processed/`（`cache_manifest.py:33`，`build_features.py:180`，`market_wide_storage.py:39`）。路径字符串在 5 个模块各自硬编码：train_panel_panel.py、cache_manifest.py、build_features.py、market_wide_storage.py、data_quality_gate.py。
- **§七 data_manifest_hash 只含 daily**：`_panel_store_meta`（`train_panel_panel.py:119-211`）写 horizon/seq_len/start/end/universe/n_stocks/feature_switches/config_hash/git_commit/data_manifest_hash(:172)/calendar_hash(:174,181)/universe_status_hash(:185)/membership_hash(:191, CSI-only)/universe_membership(:199)/prebuilt_feature_manifest_hash(:202)/entry_fill_prob_mean(:210)。`panel_store.py` `_CRITICAL_META_KEYS`(:100-103) 8 项 hard-fail，`_WARN_META_KEYS`(:114-117) 5 项外部 artifact hash。`_SELF_CONSISTENCY_RECOMPUTE`(:336-339) 只 {feature_schema_hash, stock_order_hash}。
- **§八 market_env 生命周期断裂**：生成 `a_shares/market_breadth/market_env_daily.parquet` 的代码仍在 `scripts/maintenance/legacy/_preprocess_market_env.py`（文件自标 ARCHIVED/NOT part of canonical pipeline）；legacy 对月度账户 "2015-04" 直接转 "2015-04-28"（代理日期）forward-fill。`headline_v1` 却要求 market_env `date_coverage >= 0.95`。
- **§十一 3 个 downloader 未定义 data_dir**：`download_market_breadth.py` main():22-64 调用 :55-60、`download_index_constituent.py` main():22-74 调用 :42-49 与 :65-72、`download_earnings.py` main():79-161 调用 :152-159，都 `write_run_manifest(data_dir, ...)` 传未定义名 → NameError 被 `try/except Exception` 吞掉 → 脚本 exit 0 且 manifest 静默丢失。
- **§四 streaming scratch 落系统 Temp**：`panel_builder.py` `_build_panel_streaming`(:192-477) `scratch = tempfile.mkdtemp(prefix="panel_stream_scratch_")`(:217)，per-stock pickle `{scratch}/{code}.pkl`(:241-242 写 / :305-306,324-325,373-374,389-390 读)，`shutil.rmtree` in finally(:477)。`build_panel_features`(:480-492, `memmap_dir: str|None=None` at :491) 在 `memmap_dir is not None` 时路由 streaming(:678-684)。train_panel_panel.py:350 传 `memmap_dir=args.panel_store`。`_early_panel_memory_guard`(:646-687)/`_streaming_peak_memory_gb`(:431-450) 已存在。train_panel.py **无任何 `--scratch-dir` flag**。
- **§十 coverage 已统一 metric 但缺观察范围**：`config/feature_profile.py` `CoverageContract(metric, threshold)`(:139-159)，metric ∈ {stock/cell/date/valid_state_cell}(:133-136)；`FEATURE_PROFILES["headline_v1"]`(:201-226) required_channels(:204-208) + coverage_contracts(:209-223) + `vintage_policy="safe-only"`(:224)。稀疏事件 channel 把「2025 有一篇新闻」计为 loaded → stock_coverage 100%，2005-2024 是 no_event 还是 not_observed 无法区分。
- **§九 vintage 三维已存在但 pit_alignment 不 gate**：`channel_vintage.py` `KNOWN_SOURCE_VINTAGES`(:428)/`KNOWN_TRANSFORMS`(:433)/`KNOWN_PIT_ALIGNMENTS`(:439) 各含保留 `"unknown"`；`CHANNEL_VINTAGE`(:134-409) 28 通道。immutable_snapshot/formula_versioned/proxy 的通道：**daily_qfq**(:278)、market_env(:292)、industry(:302)；capital_flow 是 formula_versioned/**verified**(:203)。`vintage_policy.py` `channel_allowed` 检查 source+transform 两层，`pit_alignment` 仅记录不 gate（§九正是要收紧这一点）。枚举：`SAFE_ONLY="safe-only"` / `ALLOW_REVISED="allow-revised"`。
- **§十九-9 formal unprobeable 已 FAIL**：`train_panel_gates.py` `_enforce_channel_coverage`(:137-206) formal 下 unprobeable → `sys.exit(1)`(:177)；zero/below-threshold abort(:197-206)。**§十九-9 大部分已满足**——本轮缺的是 manifest 强制验证（T1 补）。
- **§P2-13 剩余顶层 optional import**：`data/sources/a_shares/sector_source.py:18`、`minute_source_tencent.py:18`、`minute_source_sina_direct.py:16` 顶层 `import requests`；`scripts/production/download_data.py:9` 顶层 `import akshare as ak`。curl_cffi/bs4/fitz/playwright 已全部惰性化。
- **§P2-15 signature 已含部分维度**：`train_panel_registry.py` `_experiment_signature`(:312-425) 已 hash vintage_policy(:400)/universe_membership(:408-410)/feature_profile(:411)/calendar_artifact_hash(:379-380)；**缺** coverage contracts 细节与 asset identity。
- **§P2-14 train_panel.py 1398 行**：唯一顶层函数 `main()`(:162-1398)；已拆 7 个 satellite（folds/gates/model/oos/panel/registry/universe.py）；剩余 argparse(:163-335)、_ABLATIONS(:149-159)、gate 调用、experiment wiring(:662-666)、fold loop、OOS backtest、summary 写入(:至1398)。
- **§十二 DataAssetContract 已覆盖多数通道**：fundamental/etf/announcement/news/guba/comment/market_wide(margin/northbound/dragon_tiger/capital_flow/block_trade/lockup/dividend)/generation_store 已采用；`broadcast_assets.py:26,34` 定义 INDUSTRY_ASSET/MARKET_ENV_ASSET 但写入端是脚本（download_industry.py / 旧 _preprocess_market_env.py）非 storage 类；**未覆盖**：earnings_storage、industry/market_env 写入端、cninfo sentiment（无 storage/manifest）。

---

## 第一批：P0 — 正式重新下载/重建/大训练前（研究正确性）

### T1 — 修 3 个 production downloader 的未定义 data_dir（§十一，Low）
- **范围**：`download_market_breadth.py:55-60`、`download_index_constituent.py:42-49/65-72`、`download_earnings.py:152-159`。
  - 在 `main()` 里补 `data_dir = cfg.project.data_dir`（三个脚本都只定义了 `cfg`/`out_dir`）。
  - Manifest 写失败 → **任务非成功**：`logger.error` + `sys.exit(1)`，不再 `except Exception` 静默吞（`download_index_constituent` 的 empty-fetch 分支保持现有空数据返回语义，但 manifest 写失败必须失败）。
  - 统一 extract 一个小 helper（如 `_write_run_manifest(cfg, out_dir, ...)` 返回成功布尔，失败即 exit 1），三个脚本复用。
- **验证**：合成测试（monkeypatch 让 manifest 写抛异常）→ 脚本 exit 非 0；正常路径 exit 0 且 manifest 落盘。补 pytest 覆盖三脚本。
- **依赖**：无（隔离）。
- **风险**：Low。

### T2 — 统一 ChannelSourceSpec：消灭 live/prebuilt 路径分叉 + Golden Test（§六，HIGH RISK）
- **范围**：建**唯一** ChannelSourceSpec 注册表 `stoke_ml/data/channel_sources.py`（或复用 `asset_contract.py` 归属），形如：
  ```python
  @dataclass(frozen=True)
  class ChannelSourceSpec:
      channel: str            # "capital_flow"
      live_dir: str           # "a_shares/capital_flow"   (load_aux_data / MarketWideStorage)
      processed_dir: str | None  # "a_shares/capital_flow_processed" (cache_manifest / build_features)
      storage_kind: str       # "market_wide" | "news" | "guba" | ...
  CHANNEL_SOURCE: dict[str, ChannelSourceSpec] = { ... }
  ```
  覆盖 `headline_v1` 全部 required channel（sentiment/guba/comment/announcement/margin/northbound/dragon_tiger/capital_flow/etf_flow/block_trade/lockup/dividend/industry/market_env）+ 现有 `*_processed` 通道（board/sector/pledge/index_membership/concept）。
- **消费端全部改从 `CHANNEL_SOURCE` 取路径，禁止裸字符串**：
  - `train_panel_panel.py` `load_aux_data` 的 MarketWideStorage loop(:943-949) 与广播 probe(:1008-1041)
  - `stoke_ml/features/cache_manifest.py` `SOURCE_SUBDIRS`(:22-43)/`source_paths`(:399)
  - `scripts/production/build_features.py` `build_one()`(:168-187) 的 10 个 `*_processed` 字面量
  - `stoke_ml/data/market_wide_storage.py` `MARKET_DATA_TYPES`(:39-41)（`_processed` 变体来源）
  - `data_quality_gate.py` `AUX_PCT_DIRS`(:168-175)
- **Golden Test**：同一份 synthetic raw data → 分别走 Live Feature（`load_aux_data` + FeaturePipeline）与 Prebuilt Feature（`build_features` + FeaturePipeline）→ 最终 `past_known`/`past_observed` **逐元素相同**。若测试暴露真实差异（raw vs processed 预处理不同），在测试里显式记录 diff 原因并在任务内 reconciliation。
- **验证**：Golden Test 通过；全仓 grep 无残留裸 channel 路径字符串；`test_cache_manifest`/`test_build_features` 不破。
- **依赖**：先于 T4（T4 的 formal load_aux_data 用 CHANNEL_SOURCE 定位 storage）。
- **风险**：High（5 模块改路径来源 + Golden Test 可能暴露真实差异）。

### T3 — Vintage 语义：safe-only 改名 revision-safe + 新增 headline-strict 档（§九，Med）
- **范围**（用户已确认「两者都做」）：
  - `stoke_ml/data/vintage_policy.py`：`VintagePolicy` 枚举改三档：
    - `REVISION_SAFE = "revision-safe"`（原 `SAFE_ONLY = "safe-only"`，source 层 admission 语义不变）
    - `ALLOW_REVISED = "allow-revised"`
    - `HEADLINE_STRICT = "headline-strict"`（新增：`source != latest_revised` 且 `transform != unknown` 且 **`pit_alignment == "verified"`**；对白名单特殊通道（scale-invariant QFQ 等）显式 waiver，waiver 白名单常量化 + 注释 rationale）
  - `channel_allowed`/`allowed_channels`/`denied_channels`/`vintage_report` 支持新档位；`pit_alignment` 从「仅记录」升级为 headline-strict 的 admission gate（revision-safe/allow-revised 仍只查 source+transform）。
  - **涟漪**：`train_panel_universe.py` CLI `--vintage-policy` 的 choices；`feature_profile.py` `headline_v1` `vintage_policy="safe-only"` → `"revision-safe"`；所有 `VintagePolicy.SAFE_ONLY` 引用；测试；文档（CLAUDE.md/CONTEXT.md/README.md 的 vintage 段落）；experiment signature 值（T13 涉及）。
  - 向后兼容：旧 store/实验记录的 `"safe-only"` 字符串在 strict 读时映射到 `"revision-safe"`（别名映射），避免旧 store 全拒。
- **验证**：policy 矩阵 pytest 覆盖三档 + waiver 白名单；rename 后全仓 grep 无 `SAFE_ONLY`/`"safe-only"` 残留；`test_vintage_policy.py` 全绿；docs 门禁更新后 71/71（新增术语）。
- **依赖**：先于 T4（T4 的 formal manifest 校验要匹配新 vintage 档）。
- **风险**：Med（跨文件重命名 + 语义收紧；别名映射保旧 store）。

### T4 — Formal `load_aux_data()` 强制 required Channel 验证 Asset Manifest（§五 + §十九-9，Med）
- **范围**：`train_panel_panel.py` `load_aux_data` 加 `formal: bool = False` 参数；`_resolve_panel` 的 live 分支（:311 `if not args.no_aux and not args.prebuilt:`）把 `formal=_formal_mode(args)` 传入。
  - formal=True 时，对每个 required channel（`_resolve_required_set` 输出）走对应 storage 的 `check_asset_read(..., require_valid_manifest=True)`（或等价的 formal read）：manifest 存在 + content hash 匹配 + schema 匹配 + vintage declaration 匹配 + coverage contract 匹配，任一失败 → **raise/FAIL**（不是 warning）。
  - 无 manifest 支持的通道（cninfo sentiment 路径）formal → FAIL（提示改用 prebuilt 或补 manifest）。
  - 复用 `asset_contract.py` 的 `validate_asset_manifest`/`check_asset_read`(:404-477/:480-513)；缺 manifest 支持的通道逐个补 write 端 manifest（协调 T5/T9）。
  - 补 §十九-9 的收口验证：formal 下 required channel 缺 manifest / manifest 失配 → 硬失败（现有 coverage gate 已对 unprobeable 硬失败，本轮补 manifest 侧）。
- **验证**：formal 下篡改/删除某 required channel 的 manifest → 训练入口 FAIL；explore 模式 → warning proceed。pytest 覆盖 manifest 缺失/内容失配/覆盖失配三态。
- **依赖**：T2（CHANNEL_SOURCE 定位 storage）、T3（vintage 档位匹配）。
- **风险**：Med（formal 默认开会让旧 aux 数据被拒——预期；explore 逃生口保留）。

### T5 — 正式 `build_market_env.py` + 拆 market_env_price / market_env_account（§八，Med）
- **范围**：`scripts/production/build_market_env.py` 替代 `scripts/maintenance/legacy/_preprocess_market_env.py`（保留 legacy 只读不动，标注 superseded）。
  - **market_env_price**（当日交易数据，safe）：high/low breadth、market turnover、industry advance ratio → 标 `source_vintage=immutable_snapshot, transform=formula_versioned, pit_alignment=verified`。
  - **market_env_account**（时间存疑，月度账户统计：新增投资者/户均市值等）：改用真实发布日期；若拿不到真实发布日期则标 `pit_alignment=proxy` 且从 safe-only/revision-safe headline required 移除（留作 ablation 通道）。
  - 输出 `a_shares/market_breadth/market_env_daily.parquet` + 用 `broadcast_assets.py` 的 `MARKET_ENV_ASSET` 写 manifest（衔接 T9）；`feature_profile.py` headline_v1 的 market_env 相关 required/coverage 随拆分调整。
- **验证**：合成数据 → build_market_env 产出 price 部分 verified PIT、account 部分 proxy 或真实日期；headline_v1 revision-safe 下 market_env 仍 required 且可验证；manifest 落盘。
- **依赖**：T3（vintage 档位）、T2（路径注册表含 market_env）。
- **风险**：Med（拆通道影响 profile 声明与 coverage 契约）。

### T6 — PanelStore `panel_input_hash` + feature code tree hash（§七，Med）
- **范围**：
  - `_panel_store_meta`（`train_panel_panel.py:119-211`）新增 `panel_input_hash`：聚合 daily manifest + required/live channel asset manifests（T4 验证后的 asset roots）+ shared inputs + **feature code tree hash** + feature config（config_hash 已有）+ calendar_hash + universe_hash + membership_hash + feature profile + vintage policy。形如 `SHA256(canonical JSON({...}))`（canonical JSON = 排序键 + 紧凑序列化）。
  - **feature_code_tree_hash**：对 `stoke_ml/` + `scripts/production/` 源码树做内容 hash（非 git_commit），成为 PanelStore formal **hard binding**（进 `_CRITICAL_META_KEYS` 或 strict 判定），替代「只靠 git_commit warn-and-proceed」。git_commit 保留为 informational。
  - `panel_store.py` 的 `_WARN_META_KEYS`/`_CRITICAL_META_KEYS`/`_validate_meta`(strict 分支 :569-593) 接入 `panel_input_hash`；formal load mismatch → rebuild（拒绝加载）。
  - 补 §七「假 provenance」防护：live 模式构建的 store 必须绑定当日 aux asset roots，第二天 aux 变了 → formal load 拒。
- **验证**：改一个 live channel manifest 后 formal load 旧 store → 拒（提示 rebuild）；feature 代码树改动（内容 hash 变）→ formal 拒；测试覆盖 mismatch 三态。复用 `verify_panel_store_chunks` 思路的哈希一致性测试。
- **依赖**：T4（asset roots 验证产物）。
- **风险**：Med（feature code tree hash 计算成本——只对变更检测，构建时算一次，可缓存 git-tracked 文件集合）。

### T7 — Streaming Panel `--scratch-dir` + 磁盘预检 + 崩溃清理（§四/§十五，Med）
- **范围**：
  - `train_panel.py` 加 `--scratch-dir`（默认 `None` → 推导为 `<panel-store>/scratch/<run_id>/`，`<panel-store>` 即 `args.panel_store`；无 panel_store 时退化为系统 temp 或报错）。
  - `train_panel_panel.py:350` 把 scratch 路径传入 `build_panel_features(..., scratch_dir=...)` → `_build_panel_streaming`（`:217` 的 `tempfile.mkdtemp` 改为用传入 scratch_dir；`:477` cleanup 保留）。
  - **磁盘预检**：正式构建前 `shutil.disk_usage(scratch_dir)`，预估 `final_panel_size + scratch_size + safety_margin`，不足 → **拒绝启动**（复用 `_streaming_peak_memory_gb` 的估算风格；final panel ≈ `n_stocks*T*D*4B`，scratch ≈ per-stock pickle 总量）。
  - **run manifest + stale cleanup**：scratch 目录写 `run_manifest.json`（run_id/start_time/阶段/pid）；崩溃后同 run_id 重跑 resume；启动时清理超过 N 天（可配）的旧 scratch 目录。
- **验证**：磁盘不足（monkeypatch `shutil.disk_usage`）→ 拒绝启动；scratch 落在 `<panel-store>/scratch/` 而非系统 Temp；stale 目录被清理；崩溃恢复 resume 测试。
- **依赖**：无（独立于 T2/T3 的增量）。
- **风险**：Med（Windows 文件锁 / 长构建中断恢复的边界）。

---

## 第二批：P1 — 正式 OOS / Lockbox 前（研究完整性）

### T8 — Sparse Channel 观察范围：provider-era coverage（§十，Med）
- **范围**：
  - 资产 manifest 扩展记录：`provider_available_start/end`、`retrieved_ranges`、`known_gaps`（写入端：news/guba/comment/announcement 的 storage manifest；读取端解析这些字段）。
  - `config/feature_profile.py` 新增 profile 要求：`era_coverage`（provider 可观察区间内的检索覆盖）/`date_availability`；`headline_v1` 可对 sentiment/guba 加 `era_coverage >= 0.90` 之类的契约。
  - `_enforce_channel_coverage`（gates:137-206）识别 no_event vs not_observed：manifest 缺 retrieved_ranges → not_observed，不计入 stock_coverage 分子（或单独报 era_coverage）。
- **验证**：合成 manifest（有 retrieved_ranges vs 无）→ era_coverage 分别计算正确；headline_v1 新契约在 formal 下可判定。测试覆盖 news/guba 两个代表。
- **依赖**：T4（formal manifest 读取）。
- **风险**：Med（manifest 写入端需先有 retrieved_ranges 数据——下载器可能没记；无则先补记）。

### T9 — DataAssetContract 扩展到剩余 headline Channel（§十二，Med）
- **范围**：把 `asset_contract.py` 的 `DataAssetContract`/`AtomicCommit` 推广到 headline_v1 当前仍未统一的：
  - **industry**：`download_industry.py` 写 `industry_returns.parquet` 时用 `INDUSTRY_ASSET` 写 manifest + atomic write。
  - **market_env**：T5 的 `build_market_env.py` 用 `MARKET_ENV_ASSET`（T5 已含，此处收口读端 formal）。
  - **earnings_storage**：补 `EARNINGS_ASSET` manifest（earnings 是 latest_revised，headline_v1 safe 不 required，但 DataAsset 治理统一）。
  - **cninfo sentiment**：无 storage/manifest——formal 读路径拒绝（明确提示）或补最小 manifest；T4 已定 formal 下缺 manifest → FAIL，此处补写入端或显式排除。
  - 统一 Formal Asset protocol 文档化（identity/content hash/schema/effective-time/vintage/coverage/atomic/manifest/formal read）。
- **验证**：每新增通道 round-trip + 篡改 manifest 的 pytest；`check_asset_read(require_valid_manifest=True)` 对每通道可用。
- **依赖**：T5（market_env 写入端）。
- **风险**：Med（多 storage 世代逐个收敛）。

### T10 — Lockbox 前完整 chunk verification CLI + 协议步骤（§十九-12，Low）
- **范围**：`verify_panel_store_chunks(out_dir)`（v16 T10b 已实现）暴露为正式入口：
  - 加 `scripts/production/verify_panel_store.py`（或 `train_panel.py --verify-panel-store <dir>`）：对给定 store 跑深度 chunk 重 hash，输出 `{verified, arrays_verified, chunks_verified}`，失败 exit 非 0。
  - 在 CLAUDE.md/CONTEXT.md 研究协议段落写明：**Lockbox 前对每个 PanelStore 主动执行一次完整 chunk verification**（P1 precondition）。
- **验证**：CLI 对干净 store exit 0；对篡改 store exit 非 0（复用现有篡改测试）。
- **依赖**：无。
- **风险**：Low。

---

## 第三批：P2 — 工程优化

### T11 — 清理在线 Provider 顶层 optional import（§P2-13，Low）
- **范围**：`data/sources/a_shares/sector_source.py:18`、`minute_source_tencent.py:18`、`minute_source_sina_direct.py:16` 顶层 `import requests` 惰性化（移入使用它的函数，`import requests` 放 try/except 或函数内）；`scripts/production/download_data.py:9` 顶层 `import akshare as ak` 移入需要它的分支。
- **验证**：无 requests 环境 import 数据链成功（subprocess 测试，仿 `test_train_panel_lazy_pyarrow.py` 模式）；现有测试全绿。
- **依赖**：无。
- **风险**：Low。

### T12 — 继续拆 `train_panel.py` + Quality Gate（§P2-14，Low）
- **范围**：`train_panel.py`（1398 行，唯一 `main()`:162-1398）继续拆 satellite：
  - OOS backtest + summary 写入段 → `train_panel_oos.py` 或新 `train_panel_summary.py`。
  - fold loop → `train_panel_folds.py`（已有，把剩余 fold 循环体并入）。
  - argparse 段(:163-335) → `train_panel_cli.py`（新增，构建 `ArgumentParser` + 返回 args）。
  - `train_panel_gates.py` 的 `_require_quality_gate`(:208-371) 拆成 gate 报告解析/校验两个函数（或拆 `_gate_report.py`）。
  - **纯重构，行为不变**：测试全绿 + import 链兼容（保留 re-export 别名，防 satellite 交叉 import 破坏）。
- **验证**：`pytest tests/` 全绿；`import train_panel` 链成功；无重复逻辑（grep 关键函数单定义）。
- **依赖**：无。
- **风险**：Low（纯重构，注意 re-export 兼容）。

### T13 — FeatureProfile/Vintage/Coverage/AssetIdentity 全部进 Experiment Signature（§P2-15，Low）
- **范围**：`train_panel_registry.py` `_experiment_signature`(:312-425) 追加：
  - **coverage contracts 细节**（`feature_profile` 已 hash 名字，补 contracts 内容 hash——`coverage_contracts` dict 的 canonical JSON）。
  - **asset identity**（本实验采用的 DataAssetContract 集合 / asset contract 版本）进 signature。
  - 与已有 vintage_policy(:400)/universe_membership(:408-410)/feature_profile(:411) 保持同模式。
- **验证**：两 config 仅 coverage_contracts 不同 → signature 不同；`test_signature_binds_*` 扩展钉死。
- **依赖**：T8（coverage 契约语义落地）。
- **风险**：Low。

### T14 — 正式研究统一 Prebuilt 主线（§十三/§P2-16，Low）
- **范围**：
  - formal 模式下大型 universe（如 > 某阈值股票数）强制 `--prebuilt`：live Feature 只在 debug/smoke/小 universe/feature 开发用。formal + live + 大 universe → 拒绝启动并提示用 prebuilt。
  - 文档化 canonical 流程：Raw Assets → Formal Asset Gate → Prebuilt Feature Artifact → Formal Feature Manifest → Streaming PanelStore → Training；写进 CONTEXT.md 研究协议段。
  - 不删除 live 路径（保留 debug 能力），只做 formal 门禁 + 文档。
- **验证**：formal + `--universe all` + 无 `--prebuilt` → 拒绝并提示；debug 模式不受影响；docs 更新后门禁 71/71。
- **依赖**：T4（formal manifest 强制）。
- **风险**：Low（策略门禁 + 文档）。

---

## 明确不做的（推迟项）

- **官方 effective-date CSI membership 数据**（§十四 / §十九-10）：用户已确认**标记暂缓**——属外部数据获取项目（需官方/可靠第三方源），当前月度重建已显式声明 provenance（v16 T6），不阻塞 v17 代码修复。记 KB backlog：`01-Projects/Stoke_MachineLearning.md` 续接点添加「换官方 effective-date membership」数据获取项。
- **新模型 / 继续加特征**（§十八 audit 明确建议冻结研究协议）：本轮不做。
- **v16 已推迟的 Raw OHLC + Adjustment Factor 存储重构**：继续推迟。

## 执行顺序

1. **第一批 P0**：T1 → T2 → T3 → T4 → T5 → T6 → T7 串行。硬顺序：T2（路径注册表）先于 T4；T3（vintage 档）先于 T4（formal manifest 匹配 vintage）；T5（market_env 写入端）先于 T9。
2. **第二批 P1**：T8 → T9 → T10 串行（T8 依赖 T4；T9 依赖 T5）。
3. **第三批 P2**：T11 → T12 → T13 → T14 串行（T13 依赖 T8；T14 依赖 T4）。
4. 串行 subagent 流程天然无文件冲突。每项含专项测试。

## 端到端验证

每批完成后跑全仓测试确认无回归：
```bash
PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/ -q                      # fast smoke
PYTHONPATH=. ./.venv/Scripts/python -m pytest -m "" tests/ -q                # everything (~19min)
PYTHONPATH=. ./.venv/Scripts/python scripts/production/check_docs_consistency.py
```
每 Task 内的专项验证见各 Task 描述。三批全完后：全量测试 + docs 门禁 + KB 沉淀（v17 ADRs）→ 冻结研究协议，进入 Baseline → Ablation → Continuous OOS → Lockbox。
