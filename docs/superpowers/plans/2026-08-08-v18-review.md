# v18 审核修复计划（V11 后一轮 · 十项）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `ChatGPT_v18.md` §二十 的 10 项，把 Formal 语义从「有防护机制」推进到「防护机制的定义严格一致」——Profile/Vintage 不可拆分、Manifest Gate 覆盖全部 consumed channel、era_coverage 不能被 10/500 股票绕过、Streaming 磁盘检查按文件系统、Derived Asset 血缘完整、fingerprint 走 manifest 聚合。

**Architecture:** 不改模型/账户/Loss（§十八 无红灯）。本轮全部是 Data identity / Feature recipe / PIT-Vintage / Asset lifecycle 语义收紧：T1 把 named FeatureProfile 绑定自己的 VintagePolicy；T2 把 Formal Manifest Gate 从 required 扩到 consumed；T3 修 era_coverage 的观察范围漏洞；T5 修 market_env 上游 + 强制 PRICE schema；T6-T10 是磁盘/血缘/fingerprint/manifest 治理。正式训练入口仍是 `train_panel.py`（satellite：gates/panel/universe），数据契约走 `stoke_ml/data/asset_contract.py` + `stoke_ml/config/feature_profile.py`。

**Tech Stack:** Python 3.12 / pandas / numpy / pyarrow / parquet（lz4）/ pytest。全部改动在 `scripts/production/` 与 `stoke_ml/{config,data}`。

---

## Context — 已核实的代码事实（直接读当前 HEAD，非仅审计转述）

### 用户决策（已确认）
- **范围**：全 10 项（§二十 1–10）。
- **T4**：**维持 `>1000` 阈值**——不把 Formal 全部强制 prebuilt（audit §九 建议全强制，用户否决），只补文档。
- **T9**：**补文档声明**——不新建 `headline_strict_v1` profile，只声明 revision-safe headline_v1 含 industry proxy。
- 修完 T1–T5 后**停止大规模架构修改**（audit 原话「前五项完成以后就可以停止大规模系统架构修改」）。

### 代码事实

- **`_resolve_required_set(args)`（`scripts/production/train_panel_gates.py:101-135`）**：`active = _gate_enforced and _formal_mode and profile_name not in (None,"","none")`；active 时 `profile = profile_for(profile_name)`，未知 profile → SystemExit；`required_set = set(profile.required_channels) | extra`。**无 `args.vintage_policy == profile.vintage_policy` 检查**（T1 目标）。`--allow-fundamental-ablation` 由 `_panel_pipeline_kwargs`（`train_panel_panel.py:125-126`）强制 `use_fundamental=True`，但**不加入 required_set**（T1/T2 目标）。
- **`_panel_pipeline_kwargs(args, seq_len)`（`train_panel_panel.py:100-129`）**：单一真源——`{_SWITCH_KEY.get(dim, f"use_{dim}"): pref and channel_allowed(dim, policy)}` over `_BASE_DIM_PREFERENCE`（:82-96）。`_SWITCH_KEY = {"announcement": "use_announcements"}`（:98）。这是 T2 `_consumed_channels` 的派生基础。
- **`_enforce_formal_manifests(stock_list, data_dir, start_date, end_date, required_set)`（`train_panel_panel.py:1373-1520`）**：`for ch in sorted(required_set)`（:1446）遍历；分支 sentiment/guba/comment/fundamental/announcement（cninfo 无 manifest → fail）/`_MARKET_WIDE_CHANNELS`/etf_flow/industry+market_env；else → problems.append「required channel is not loaded by load_aux_data」。**只覆盖 required，不覆盖 consumed**（T2 目标）。
- **`load_aux_data(stock_list, data_dir, start_date, end_date, required_channels=None, formal=False)`（`train_panel_panel.py:1523-1730`）**：`if formal and required_set: _enforce_formal_manifests(...)`（:1560-1562）。`_resolve_panel` live 分支（:644-648）调用时传 `required_channels=required_set, formal=_formal_mode(args)`。
- **`_aux_asset_root_hash(data_dir, required_set, *, live_aux)`（`train_panel_panel.py:203-228`）**：对 `sorted(required_set)` 的 `_asset_manifest_entries` 聚合。`_panel_store_meta`（:358-360）传 `required_set`。**只 hash required，不 hash consumed**（T2 目标）。
- **`_probe_era_coverage(data_dir, channel, stock_list)`（`train_panel_panel.py:491-511`）**：返回 `(mean_coverage, era_observable_stocks, era_not_observed_stocks)`；mean 只对 era-observable stocks 求。**10/500 构造：mean=1.0, n_obs=10, n_not=490 → 通过**（T3 目标）。`_merge_era_coverage`（:514-545）写 `era_observable_stocks/era_not_observed_stocks/era_coverage`。`_era_capable_channels`（:419-439）从 profile contracts 派生 `metric=="era_coverage"`。
- **`_enforce_channel_coverage(required_set, channel_manifest, coverage_contracts=None, formal=False)`（`train_panel_gates.py:137-215`）**：对每个 required channel 读 `contract.metric`（默认 stock_coverage）→ unprobeable/zero/below-minimum abort。`CoverageContract(metric, threshold)`（`stoke_ml/config/feature_profile.py:226-246`），`_COVERAGE_METRICS` = {stock/cell/date/valid_state_cell/era_coverage/date_availability}（:220-223）。**无复合契约（era_coverage AND era_observable_stock_fraction）**（T3 目标）。
- **`headline_v1`（`feature_profile.py:302-327`）**：required_channels 14 个；contracts：sentiment/guba = `era_coverage 0.90`，其余 stock/date_coverage；`vintage_policy="revision-safe"`。`MARKET_ENV_PRICE_COLS` = {high_low_ratio, market_adv_ratio, market_turnover_z}（:101-103）。
- **`build_market_env.py`**：`build_industry_advance(base)`（:113-130）读 `a_shares/industry/industry_ranking_computed.parquet`，期望 `date`/`ind_return`——**真实上游是 `download_industry_ranking.py` 写的 `a_shares/industry_ranking.parquet`（date/sector_code/change_pct 等列，output_path 在 download_industry_ranking.py:37）**（T5 目标）。`build_market_env`（:187-255）对每个 part「source 非空才加列」→ PRICE 列可能天然缺失仍能写出+有效 manifest（T5 目标：断言 `set(MARKET_ENV_PRICE_COLS) <= set(df.columns)`）。`write_market_env(data_dir, df, parts)`（:272-289）调 `write_asset_manifest(out_path, MARKET_ENV_ASSET, df, parts=parts)`，**无 upstream lineage**（T7 目标）。
- **`MARKET_ENV_ASSET`（`stoke_ml/data/broadcast_assets.py:34-40`）**：`contract_for_channel("market_env", data_type="market_env_daily", partition="single_file", extent_column="date", effective_date_policy="index_date")`。`contract_for_channel` 支持 `column_contract=`（`asset_contract.py:249-277`），**当前未传**（T5 目标）。`DataContract`（`contract.py:571-637` 的 MARGIN 范式）有 dataset_name/primary_key/required_columns/units/price_basis/timezone/calendar/source_priority。
- **`_enforce_streaming_disk_space`（`train_panel_panel.py:1116-1152`）**：`required_gb = _streaming_disk_required_gb(*est)`（final+scratch+margin，:1057-1069）对 `shutil.disk_usage(scratch_dir).free` 比较——**只查 scratch 盘，跨盘时假失败/假通过**（T6 目标）。panel_builder 兜底（`stoke_ml/features/panel_builder.py:593-613`）：`need = grid_bytes + scratch_bytes + margin` 对 `shutil.disk_usage(scratch).free`——同病（T6 目标）。
- **`dataset_fingerprint(root, datasets)`（`scripts/production/data_quality_gate.py:319-375`）**：`_hash_file` 逐块读全 parquet bytes（`daily` = `root/a_shares/daily`）。`QUALITY_GATE_VERSION = "2.0"`（:189）。gate report 写 `data_manifest_hash`（`data_quality_gate_run.py:291`），train_panel_gates 校验（:312）——两侧同函数，改算法两侧同步变（**T8 目标 + version bump**）。daily 侧车是 `daily/{code}.manifest.json`（`stoke_ml/data/storage.py:249`，含 content 型 `schema_hash`）。
- **`_manifest_body_digest(path)`（`train_panel_panel.py:150-177`）**：manifest JSON 去掉 `written_at` 后 hash；非 JSON → 原始字节 hash；不可读 → `"<unreadable>"`。可复用到 T8（移到 `asset_contract.py` 供两处共享）。
- **`write_run_manifest` failure 仍是 warning**：`download_industry.py:88-96` 与 `download_industry_ranking.py:159-167` 都是 `except Exception as exc: logger.warning("run manifest write failed: %s", exc)`（T10 目标）。
- **`hash_json(obj)`** 位于 `stoke_ml/models/panel/code_tree_hash.py:49`（canonical JSON + SHA256），T7 的 `transform_config_hash` 用它。
- **测试基座**：`tests/scripts/test_train_panel_universe.py` 的 `_panel_args(vintage_policy, **overrides)`（:1385-1406）默认 `feature_profile=None`、`no_formal=False`、`no_require_quality_gate=False`、`allow_fundamental_ablation=False`；`_resolve_required_set` 既有测试（:1706-1774）用 `feature_profile="headline_v1"` + `vintage_policy="revision-safe"`（匹配，T1 不破）；`_panel_store_meta(..., required_set=set())` 调用点 ~14 处（test_train_panel_universe.py / test_panel_store.py / test_panel_input_hash.py）。`tests/scripts/test_build_market_env.py` `_write_industry` 写 `industry_ranking_computed.parquet`（T5 需改）。`tests/data/test_data_quality_gate.py:763-781` 的 fingerprint 测试（T8 需改）。

---

## 第一批：P0 — Formal 语义一致（T1–T5，修完即停止大规模架构修改）

### T1 — Formal named FeatureProfile 强制绑定自己的 VintagePolicy + ablation 加入 required（§二十-1，Med）

**Files:**
- Modify: `scripts/production/train_panel_gates.py:101-135`
- Test: `tests/scripts/test_train_panel_universe.py`

- [ ] **Step 1: 写失败测试**——在 `test_train_panel_universe.py` 的 §十四 区追加：

```python
def test_resolve_required_set_mismatched_vintage_aborts(tp):
    """§v18-1: a named profile is ONE indivisible research recipe — the run's
    --vintage-policy MUST equal the profile's declared vintage_policy, or the
    required set (resolved under the profile) would not match the channels the
    pipeline actually opens.  headline_v1 declares revision-safe."""
    args = _panel_args("allow-revised", feature_profile="headline_v1")
    with pytest.raises(SystemExit) as ei:
        tp._resolve_required_set(args)
    assert "vintage" in str(ei.value).lower()
    assert "headline_v1" in str(ei.value)

def test_resolve_required_set_matching_vintage_ok(tp):
    """The default revision-safe + headline_v1 combination stays valid."""
    args = _panel_args("revision-safe", feature_profile="headline_v1")
    required_set, contracts, name = tp._resolve_required_set(args)
    assert name == "headline_v1"

def test_resolve_required_set_ablation_adds_fundamental(tp):
    """§v18-1: --allow-fundamental-ablation forces fundamental ON — the same
    class of hole as a mismatched vintage.  Under an active profile the flag
    ADDS fundamental to required_set so the gate covers it."""
    args = _panel_args("revision-safe", feature_profile="headline_v1",
                       allow_fundamental_ablation=True)
    required_set, _, name = tp._resolve_required_set(args)
    assert name == "headline_v1"
    assert "fundamental" in required_set

def test_resolve_required_set_ablation_no_profile_unchanged(tp):
    """No active profile → the ablation flag only affects the pipeline switch,
    not the required set (existing behavior)."""
    args = _panel_args("revision-safe", feature_profile="none",
                       allow_fundamental_ablation=True, require_aux_channels="guba")
    required_set, _, name = tp._resolve_required_set(args)
    assert name == "none"
    assert required_set == {"guba"}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_train_panel_universe.py -k "mismatched_vintage or matching_vintage or ablation_adds or ablation_no_profile" -v`
Expected: `test_resolve_required_set_mismatched_vintage_aborts` 与 `test_resolve_required_set_ablation_adds_fundamental` FAIL（不 abort / 不加 fundamental）。

- [ ] **Step 3: 实现**——改 `_resolve_required_set`，在 `profile = profile_for(profile_name)` 与 `if profile is None` 之后插入：

```python
    # §v18-1: a named profile is ONE indivisible research recipe — the run's
    # --vintage-policy MUST equal the profile's declared vintage_policy, or the
    # required-channel set (resolved under the profile) would not match the
    # channels the pipeline actually opens (headline-strict closes industry;
    # allow-revised opens 8 more latest_revised channels).  Refuse loudly — a
    # mismatched vintage silently gates a different recipe than the one the
    # model consumes.
    args_vintage = getattr(args, "vintage_policy", None)
    if args_vintage != profile.vintage_policy:
        raise SystemExit(
            f"--feature-profile {profile_name!r} declares vintage_policy "
            f"{profile.vintage_policy!r} but --vintage-policy is "
            f"{args_vintage!r} — a named profile and its vintage policy are "
            f"one indivisible research recipe (§v18-1).  Use --feature-profile "
            f"none for a custom vintage policy, or align --vintage-policy to "
            f"the profile.")
    required_set = set(profile.required_channels) | extra
    # §v18-1: --allow-fundamental-ablation forces the fundamental channel ON
    # regardless of policy — the same class of hole as a mismatched vintage.  A
    # channel the model actually consumes must be REQUIRED (gated), so under an
    # active profile the ablation flag ADDS fundamental to the required set
    # rather than letting it ride ungated.
    if getattr(args, "allow_fundamental_ablation", False):
        required_set.add("fundamental")
    return required_set, dict(profile.coverage_contracts), profile_name
```

- [ ] **Step 4: 跑测试确认通过**

Run: 同 Step 2，再加既有 §十四 区全量：
Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_train_panel_universe.py -q`
Expected: 全绿（既有 `_resolve_required_set_*` 用 revision-safe+headline_v1，匹配，不破）。

- [ ] **Step 5: Commit**

```bash
git add scripts/production/train_panel_gates.py tests/scripts/test_train_panel_universe.py
git commit -m "fix(v18-T1): bind named FeatureProfile to its declared VintagePolicy; ablation adds fundamental to required set (§二十-1)"
```

**依赖**：无（隔离）。**风险**：Med（CLI 默认 revision-safe+headline_v1 匹配，不破默认路径；仅 mismatch 配置被拒）。

---

### T2 — Formal Manifest Gate 从 required 扩到所有 consumed channel（§二十-2，Med/High）

**Files:**
- Modify: `scripts/production/train_panel_panel.py`（`_consumed_channels` 新增、`_enforce_formal_manifests` :1373、`load_aux_data` :1523、`_aux_asset_root_hash` :203、`_panel_store_meta` :231、`_resolve_panel` :644）
- Modify: `stoke_ml/data/asset_contract.py`（新增公开 `manifest_body_digest`）
- Test: `tests/models/panel/test_panel_input_hash.py`、`tests/scripts/test_train_panel_formal_manifest.py`、`tests/scripts/test_train_panel_universe.py`

**核心模型**：Consumed = 模型实际读的（必须有效 manifest + 进 Panel input identity）；Required ⊂ Consumed = 再附加 coverage threshold。Manifest Gate 与 aux_asset_root_hash 都绑 **consumed**；coverage gate 仍按 required + contracts。

- [ ] **Step 1: 把 `_manifest_body_digest` 移到 `asset_contract.py`**（T8 也要用，放数据契约层）：

在 `asset_contract.py` 的 `atomic_write_json` 之后加：

```python
def manifest_body_digest(path: str) -> str:
    """Content digest of ONE ``*.manifest.json`` sidecar, ``written_at`` excluded.

    ``written_at`` is a per-write TIMESTAMP, so a content-identical rewrite of
    the same data (a re-download that changed nothing) must NOT change any
    hash built from this sidecar — the binding is WHAT the data is, not when it
    was written.  The sidecar is parsed as JSON and the timestamp key dropped
    before the canonical digest; a non-JSON / unreadable sidecar degrades to
    hashing its RAW BYTES (a partial rewrite still visible, and a JSON-level
    nicety like written_at exclusion is moot when the file is not JSON anyway).
    Returns the hex digest — never None: an unreadable sidecar maps to
    ``"<unreadable>"`` so the entry stays present (fail-closed).
    """
    try:
        with open(path, "r", encoding="utf-8") as fh:
            obj = json.load(fh)
    except (OSError, ValueError):
        try:
            h = hashlib.sha256()
            with open(path, "rb") as fh:
                for chunk in iter(lambda: fh.read(65536), b""):
                    h.update(chunk)
            return h.hexdigest()
        except OSError:
            return "<unreadable>"
    if isinstance(obj, dict):
        obj = {k: v for k, v in obj.items() if k != "written_at"}
    payload = json.dumps(obj, sort_keys=True, separators=(",", ":"),
                         ensure_ascii=False).encode("utf-8")
    return hashlib.sha256(payload).hexdigest()
```

在 `train_panel_panel.py` 删除本地 `_manifest_body_digest`（:150-177）定义，改为 import 并保留别名（`_asset_manifest_entries` 两处调用点不动）：

在 `train_panel_panel.py` 的 import 区（`from stoke_ml.data.asset_contract import ...` 处）加：
```python
from stoke_ml.data.asset_contract import manifest_body_digest as _manifest_body_digest
```

- [ ] **Step 2: 写失败测试**——`test_panel_input_hash.py` 追加（consumed 绑定 + required⊆consumed 不变量）：

```python
def test_aux_asset_root_hash_binds_consumed_not_just_required(self, tmp_path):
    """§v18-2: the aux root binding must cover the channels the run CONSUMES,
    not only the required subset — a consumed-but-unrequired channel (e.g. the
    fundamental ablation) whose data changes must make the binding differ."""
    data = self._data(tmp_path)
    h_required_only = _aux_asset_root_hash(data, {"sentiment"}, live_aux=True)
    h_with_consumed = _aux_asset_root_hash(data, {"sentiment", "fundamental"}, live_aux=True)
    assert h_required_only != h_with_consumed

def test_panel_store_meta_binds_consumed_channels(self, tmp_path):
    """§v18-2: _panel_store_meta's aux_asset_root_hash is computed over the
    CONSUMED channel set (derived from args + seq_len), so an ablation store
    differs from a non-ablation store even when required_set is empty."""
    base = _panel_store_meta(_panel_args("revision-safe"), seq_len=60,
                             stock_list=["000001"], data_dir=str(tmp_path))
    ablated = _panel_store_meta(
        _panel_args("revision-safe", allow_fundamental_ablation=True),
        seq_len=60, stock_list=["000001"], data_dir=str(tmp_path))
    assert base.get("aux_asset_root_hash") != ablated.get("aux_asset_root_hash")
```

> 若 `test_panel_input_hash.py` 没有 `_panel_args`，用 `tests/scripts/test_train_panel_universe.py` 的 `_panel_args`（或该文件本地等价 helper）。确认该文件已有 `_data` fixture（既有 `_aux_asset_root_hash` 测试用）。

- [ ] **Step 3: 跑测试确认失败**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/models/panel/test_panel_input_hash.py -k "consumed" -v`
Expected: 两个新测试 FAIL（aux hash 不区分 required/consumed；`_panel_store_meta` 仍以 required_set 计算）。

- [ ] **Step 4: 实现**

4a. 新增 `_consumed_channels`（放在 `_panel_pipeline_kwargs` 之后）：

```python
def _consumed_channels(args, seq_len: int) -> set[str]:
    """The data channels the pipeline actually OPENS for this run (§v18-2).

    Mirrors ``_panel_pipeline_kwargs``: a channel is consumed exactly when its
    ``use_*`` switch resolves True after the base preference AND the vintage
    policy AND the explicit ablation opt-ins.  Consumed ≠ Required: Required ⊂
    Consumed, with the required subset additionally carrying coverage
    contracts.  Every CONSUMED channel must have a valid asset manifest under a
    formal gate (``_enforce_formal_manifests``); the required subset is the
    extra coverage-threshold layer (``_enforce_channel_coverage``).
    """
    kwargs = _panel_pipeline_kwargs(args, seq_len)
    return {
        dim for dim in _BASE_DIM_PREFERENCE
        if kwargs.get(_SWITCH_KEY.get(dim, f"use_{dim}"), False)
    }
```

4b. `_aux_asset_root_hash` 参数 `required_set` → `consumed_set`（:203 签名 + :221 循环 + docstring 同步改「consumed」措辞）。

4c. `_panel_store_meta`：`required_set` kwarg 保留（向后兼容 + 记录契约），但 aux hash 改绑 consumed，并加 required⊆consumed 不变量：

```python
    if data_dir is not None and live_aux:
        consumed = _consumed_channels(args, seq_len)
        # §v18-2 invariant: required ⊆ consumed — a required channel the
        # pipeline does not open is a contradiction (you demand coverage for a
        # channel the model never reads).  The §v18-1 vintage binding makes
        # this hold for every named-profile run.
        not_consumed = set(required_set) - consumed
        if not_consumed:
            raise SystemExit(
                "required channels not consumed by this run's pipeline "
                f"({sorted(not_consumed)}) — required ⊂ consumed must hold; "
                "align --require-aux-channels / --feature-profile with the "
                "--vintage-policy (§v18-2)")
        meta["aux_asset_root_hash"] = _aux_asset_root_hash(
            data_dir, consumed, live_aux=True)
```

4d. `_enforce_formal_manifests`：参数 `required_set` → `consumed_set`（:1378 签名、:1446 循环 `for ch in sorted(consumed_set):`、:1511 else 分支文案「required channel」→「consumed channel」、docstring 同步）。

4e. `load_aux_data`：加 `consumed_channels: set[str] | None = None` 参数；gate 调用改用 consumed：

```python
    if formal and (consumed_channels or required_set):
        _enforce_formal_manifests(
            stock_list, data_dir, start_date, end_date,
            consumed_channels or required_set)
```

4f. `_resolve_panel` live 分支（:644-648）：

```python
        aux_data, channel_manifest = load_aux_data(
            stock_list, data_dir, args.start, args.end,
            required_channels=required_set,
            consumed_channels=_consumed_channels(args, seq_len),
            formal=_formal_mode(args),
        )
```

- [ ] **Step 5: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/models/panel/test_panel_input_hash.py tests/scripts/test_train_panel_formal_manifest.py tests/scripts/test_train_panel_universe.py -q`
Expected: 全绿。若 `_panel_store_meta` 既有测试因不变量 SystemExit 失败（传了 required 但该 vintage 下不 consumed），把该测试的 args 对齐到 consumed 现实（这正是 audit 要暴露的矛盾）。
若 `_enforce_formal_manifests` 既有测试以 `required_set=` kwarg 调用，改传 `consumed_set=`（或按位置）。

- [ ] **Step 6: 新增端到端断言**——`test_train_panel_formal_manifest.py` 加：

```python
def test_formal_manifest_gate_covers_consumed_fundamental_ablation(tp, tmp_path):
    """§v18-2: a consumed-but-unrequired channel (fundamental via ablation) is
    manifest-gated, not silently skipped — corrupting its manifest aborts."""
    # data dir with a corrupt fundamental manifest for the consumed channel
    ...  # 复用既有 _enforce_formal_manifests 测试的 storage 构造
    consumed = {"sentiment", "fundamental"}
    with pytest.raises(SystemExit):
        tp._enforce_formal_manifests(["000001"], str(tmp_path), "2020-01-01", "2024-12-31", consumed)
```

（实现者按既有 formal-manifest 测试的 fixture 补齐具体构造；核心断言：`consumed_set` 含 fundamental 时，fundamental manifest 缺失/失配 → SystemExit。）

- [ ] **Step 7: Commit**

```bash
git add scripts/production/train_panel_panel.py stoke_ml/data/asset_contract.py tests/models/panel/test_panel_input_hash.py tests/scripts/test_train_panel_formal_manifest.py tests/scripts/test_train_panel_universe.py
git commit -m "fix(v18-T2): formal manifest gate + aux root binding cover all consumed channels; required subset adds coverage (§二十-2)"
```

**依赖**：T1（T2 的 consumed 派生依赖 `_panel_pipeline_kwargs` 的 vintage 语义，T1 先锁 vintage 绑定）。**风险**：High（`_panel_store_meta` 签名行为收紧——required⊆consumed 不变量可能暴露既有测试的配置矛盾；这是 audit 想要的 fail-closed）。

---

### T3 — 修 `era_coverage`：复合契约阻止 10/500 股票代表全 Universe（§二十-3，Med）

**Files:**
- Modify: `stoke_ml/config/feature_profile.py`（`_COVERAGE_METRICS`、`CoverageContract`、headline_v1 contracts）
- Modify: `scripts/production/train_panel_panel.py`（`_merge_era_coverage` :514-545 写 fraction）
- Modify: `scripts/production/train_panel_gates.py`（`_enforce_channel_coverage` :137-215 复合检查）
- Test: `tests/scripts/test_train_panel_universe.py`、`tests/scripts/test_channel_manifest.py`

- [ ] **Step 1: 写失败测试**——`test_train_panel_universe.py` §十四 区加：

```python
def test_enforce_channel_coverage_era_fraction_blocks_10_of_500(tp, caplog):
    """§v18-3: the era-coverage contract is COMPOSITE — era_coverage >= 0.90
    AND era_observable_stock_fraction >= 0.90.  10 era-observable stocks out of
    a 500-stock requested universe (fraction 0.02) must ABORT, not pass."""
    import logging
    from stoke_ml.config.feature_profile import CoverageContract
    manifest = {
        "sentiment": {"era_coverage": 1.0, "era_observable_stocks": 10,
                      "era_not_observed_stocks": 490,
                      "era_observable_stock_fraction": 0.02},
    }
    contracts = {"sentiment": CoverageContract(
        "era_coverage", 0.90,
        requires=("era_observable_stock_fraction", 0.90))}
    with pytest.raises(SystemExit):
        tp._enforce_channel_coverage({"sentiment"}, manifest, contracts, formal=True)
    assert "era_observable_stock_fraction" in caplog.text

def test_enforce_channel_coverage_era_fraction_ok_when_450_of_500(tp):
    """450/500 era-observable (fraction 0.90) with era_coverage 0.95 passes."""
    from stoke_ml.config.feature_profile import CoverageContract
    manifest = {
        "sentiment": {"era_coverage": 0.95, "era_observable_stocks": 450,
                      "era_not_observed_stocks": 50,
                      "era_observable_stock_fraction": 0.90},
    }
    contracts = {"sentiment": CoverageContract(
        "era_coverage", 0.90, requires=("era_observable_stock_fraction", 0.90))}
    tp._enforce_channel_coverage({"sentiment"}, manifest, contracts, formal=True)
```

`test_channel_manifest.py` 的 `_probe_era_coverage` 调用点（:545/:556）解包 `(mean, n_obs, n_not)`——本任务不改 `_probe_era_coverage` 返回，只加 fraction 到 manifest entry（在 `_merge_era_coverage` 里算），故既有解包不破。

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_train_panel_universe.py -k "era_fraction" -v`
Expected: 两个新测试 FAIL（gate 不认识 `era_observable_stock_fraction` metric；`CoverageContract(requires=...)` 构造报错）。

- [ ] **Step 3: 实现**

3a. `feature_profile.py` `_COVERAGE_METRICS` 加 `"era_observable_stock_fraction"`（:220-223）。

3b. `CoverageContract` 加可选复合字段（:226-246）：

```python
@dataclass(frozen=True)
class CoverageContract:
    """Per-channel coverage contract: which metric is measured and the minimum.

    ``requires`` is an OPTIONAL COMPOSITE requirement (§v18-3): a second metric
    that must ALSO clear its own threshold.  Used by the era-coverage contract
    — the provider-era retrieval coverage AND the fraction of the requested
    universe that is era-observable must BOTH pass, so 10/500 observable stocks
    can never represent the whole universe.
    """

    metric: str
    threshold: float
    requires: tuple[str, float] | None = None

    def __post_init__(self):
        if self.metric not in _COVERAGE_METRICS:
            raise ValueError(
                f"CoverageContract metric {self.metric!r} not in "
                f"{sorted(_COVERAGE_METRICS)}")
        if not (0.0 < self.threshold <= 1.0):
            raise ValueError(
                f"CoverageContract threshold must be in (0,1], got "
                f"{self.threshold}")
        if self.requires is not None:
            req_metric, req_threshold = self.requires
            if req_metric not in _COVERAGE_METRICS:
                raise ValueError(
                    f"CoverageContract requires metric {req_metric!r} not in "
                    f"{sorted(_COVERAGE_METRICS)}")
            if not (0.0 < req_threshold <= 1.0):
                raise ValueError(
                    f"CoverageContract requires threshold must be in (0,1], "
                    f"got {req_threshold}")
```

3c. headline_v1 的 sentiment/guba contracts（:311-312）改复合：

```python
            "sentiment": CoverageContract(
                "era_coverage", 0.90,
                requires=("era_observable_stock_fraction", 0.90)),
            "guba": CoverageContract(
                "era_coverage", 0.90,
                requires=("era_observable_stock_fraction", 0.90)),
```

3d. `train_panel_panel.py` `_merge_era_coverage`（:539-545）在 entry 写 fraction：

```python
        entry = channel_manifest[ch]
        entry["era_observable_stocks"] = n_obs
        entry["era_not_observed_stocks"] = n_not
        entry["era_observable_stock_fraction"] = (
            (n_obs / len(stock_list)) if stock_list else 0.0)
        if mean_cov is not None:
            entry["era_coverage"] = mean_cov
```

3e. `train_panel_gates.py` `_enforce_channel_coverage`：在 below-minimum 检查之后加复合检查（:210-215 之后，循环体内）：

```python
        if contract is not None and contract.requires is not None:
            req_metric, req_threshold = contract.requires
            frac = entry.get(req_metric)
            if not (
                isinstance(frac, (int, float))
                and not isinstance(frac, bool)
                and np.isfinite(float(frac))
            ):
                logger.error(
                    "formal mode: required aux channel '%s' has no finite "
                    "%s probe — composite coverage cannot be verified (§v18-3)",
                    ch, req_metric)
                sys.exit(1)
            if float(frac) < req_threshold:
                logger.error(
                    "Required aux channel '%s' %s %.4f < minimum %.4f "
                    "(--feature-profile) — aborting (§v18-3)",
                    ch, req_metric, float(frac), req_threshold)
                sys.exit(1)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_train_panel_universe.py tests/scripts/test_channel_manifest.py -q`
Expected: 全绿。既有 era-coverage 测试（test_channel_manifest.py:545/:556 的 `_probe_era_coverage` 解包）不破（返回三元组未改）。

- [ ] **Step 5: Commit**

```bash
git add stoke_ml/config/feature_profile.py scripts/production/train_panel_panel.py scripts/production/train_panel_gates.py tests/scripts/test_train_panel_universe.py tests/scripts/test_channel_manifest.py
git commit -m "fix(v18-T3): composite era-coverage contract — era_coverage AND era_observable_stock_fraction both gated (§二十-3)"
```

**依赖**：无（独立）。**风险**：Med（`CoverageContract` 加字段向后兼容——既有 `CoverageContract("era_coverage", 0.90)` 无 requires 仍有效；`minimum_coverage` 投影 `.threshold` 不变）。

---

### T4 — Formal 主线文档：维持 >1000 阈值，声明 live 路径边界（§二十-4，Doc-only）

**Files:**
- Modify: `CONTEXT.md`（研究协议段落）

**决策**：audit §九 建议「Formal 全部强制 prebuilt」；**用户否决，维持 `>1000`**。`train_panel_universe.py` 的 `_PREBUILT_MAINLINE_THRESHOLD = 1000` 与 `_require_prebuilt_mainline` **不动代码**，只补文档声明两条主线的边界。

- [ ] **Step 1: 在 CONTEXT.md 研究协议段添加段落**

在 CONTEXT.md 的 Formal 研究协议段落追加（先读文件定位「研究协议」小节，在其下加）：

```markdown
### Formal 训练主线（v18 §二十-4 声明）

Formal 研究统一走 Prebuilt Feature → PanelStore → Training 主线
（Raw Assets → Formal Asset Gate → Prebuilt Feature Artifact → Feature
Manifest → Streaming PanelStore → Train）。Live FeaturePipeline 保留为
Feature 开发 / Debug / Smoke / 小规模快速实验路径。

- `--universe all`（全市场）与 formal 研究 >1000 股票（`_PREBUILT_MAINLINE_THRESHOLD`）
  强制 `--prebuilt`：live 数学输入（raw → in-memory transform）与 prebuilt
  （raw → preprocess_new_data → processed → build_features）不是同一套输入，
  formal 大实验不维护两套 Feature Source 语义。
- 阈值以下的 formal 小 universe / smoke 允许 live，作为开发路径；正式
  Lockbox 主结论一律 prebuilt。
- 不删除 live 路径——只做门禁 + 文档。
```

- [ ] **Step 2: 跑 docs 门禁**

Run: `PYTHONPATH=. ./.venv/Scripts/python scripts/production/check_docs_consistency.py`
Expected: PASS（若门禁按 78/78 计数且新增段落不引入需登记的术语，则不变；若门禁要求同步 CLAUDE.md，把同一段落的精简版加到 CLAUDE.md Known Issues 或研究协议处）。

- [ ] **Step 3: Commit**

```bash
git add CONTEXT.md CLAUDE.md
git commit -m "docs(v18-T4): declare prebuilt formal mainline + live path boundary; keep >1000 threshold (§二十-4)"
```

**依赖**：无。**风险**：Low。

---

### T5 — Market Env Formal build 强制完整 `MARKET_ENV_PRICE_COLS` + 修正 market_adv_ratio 上游 + Column Contract（§二十-5，Med）

**Files:**
- Modify: `scripts/production/build_market_env.py`（`build_industry_advance` :113-130、`build_market_env` :187-255、`main` :292-326）
- Modify: `stoke_ml/data/broadcast_assets.py`（MARKET_ENV_ASSET 加 `column_contract`）
- Modify: `stoke_ml/data/contract.py`（新增 market_env_daily DataContract）
- Test: `tests/scripts/test_build_market_env.py`（`_write_industry` 改上游）

- [ ] **Step 1: 写失败测试**——`test_build_market_env.py` 的 `_write_industry`（:70-76）从写 `industry/industry_ranking_computed.parquet`（date/ind_return）改为写真实上游 `a_shares/industry_ranking.parquet`（date/change_pct）：

```python
def _write_industry(tmp_path):
    """§v18-5: the REAL market_adv_ratio upstream is download_industry_ranking.py's
    a_shares/industry_ranking.parquet (date/sector_code/change_pct), not the
    legacy industry/industry_ranking_computed.parquet (date/ind_return)."""
    daily = tmp_path / "a_shares"
    daily.mkdir(parents=True, exist_ok=True)
    rows = pd.DataFrame({
        "date": ["2024-01-02", "2024-01-02", "2024-01-03", "2024-01-03"],
        "sector_code": ["SEC0000", "SEC0001", "SEC0000", "SEC0001"],
        "change_pct": [0.01, -0.02, 0.00, 0.03],
    })
    rows.to_parquet(daily / "industry_ranking.parquet", index=False)
    return daily
```

新增断言（`market_adv_ratio` 语义 + PRICE 缺失即失败）：

```python
def test_build_market_env_asserts_full_price_cols(tmp_path):
    """§v18-5: a builder that cannot produce the full MARKET_ENV_PRICE_COLS set
    must FAIL, not write a partial asset with a valid manifest."""
    # only industry_ranking present → high_low_ratio + market_turnover_z missing
    _write_industry(tmp_path)
    with pytest.raises(ValueError):
        bme.build_market_env(str(tmp_path))

def test_build_market_env_price_cols_from_real_upstream(tmp_path):
    """§v18-5: with all three price sources present, market_adv_ratio is computed
    from a_shares/industry_ranking.parquet (fraction of sectors with change_pct>0
    per date) and the full PRICE column set is asserted present."""
    daily = _write_industry(tmp_path)
    hl = pd.DataFrame({"date": ["2024-01-02", "2024-01-03"], "high20": [10.0, 12.0], "low20": [8.0, 9.0]})
    hl.to_parquet(daily / "market_breadth" / "highs_lows.parquet", index=False)
    # one daily parquet so turnover scan has a source
    d = pd.DataFrame({"date": ["2024-01-02", "2024-01-03"], "amount": [1e6, 2e6], "open": [10.0, 10.5], "high": [11.0, 11.5], "low": [9.0, 9.5], "close": [10.5, 11.0], "volume": [1e5, 1.2e5]})
    (daily / "daily").mkdir(exist_ok=True)
    d.to_parquet(daily / "daily" / "000001.parquet", index=False)
    df, parts = bme.build_market_env(str(tmp_path))
    assert set(bme.MARKET_ENV_PRICE_COLS) <= set(df.columns)
    # 2024-01-02: sector0 +0.01 (adv), sector1 -0.02 (decl) → 0.5
    assert abs(df.loc["2024-01-02", "market_adv_ratio"] - 0.5) < 1e-9
```

> 实现者先读 `test_build_market_env.py` 全文，按既有 fixture 对齐列名（daily 需要的列以 `_daily` helper 或既有测试的构造为准）。

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_build_market_env.py -v`
Expected: `test_build_market_env_asserts_full_price_cols` / `test_build_market_env_price_cols_from_real_upstream` FAIL（builder 现在还能产出 partial asset 且 market_adv_ratio 读错上游）。

- [ ] **Step 3: 实现**

3a. `build_industry_advance` 改读真实上游（:113-130）：

```python
def build_industry_advance(base: str) -> pd.Series:
    """Fraction of sectors with positive change_pct per date (§v18-5).

    ``base`` is the ``a_shares`` dir.  The real upstream is
    ``download_industry_ranking.py``'s ``a_shares/industry_ranking.parquet``
    (date/sector_code/change_pct — sector equal-weighted return), NOT the
    legacy ``industry/industry_ranking_computed.parquet`` (date/ind_return)
    that no production pipeline writes.  Same-day trade data → VERIFIED part.
    """
    path = os.path.join(base, "industry_ranking.parquet")
    if not os.path.exists(path):
        return pd.Series(dtype="float64")
    raw = pd.read_parquet(path)
    if "date" not in raw.columns or "change_pct" not in raw.columns:
        return pd.Series(dtype="float64")
    d = raw[["date", "change_pct"]].copy()
    d["date"] = pd.to_datetime(d["date"]).dt.normalize()
    d = d.dropna(subset=["change_pct"])
    if d.empty:
        return pd.Series(dtype="float64")
    adv = d.groupby("date")["change_pct"].apply(
        lambda x: float((x > 0).mean()))
    return adv.rename("market_adv_ratio")
```

3b. `build_market_env`（:187-255）：在 `out = pd.DataFrame(series).sort_index()` 之后、`out.index.name = "date"` 之前，加 PRICE 完整断言：

```python
    out = pd.DataFrame(series).sort_index()
    # §v18-5: the required PRICE part is atomic — a build that cannot produce
    # every MARKET_ENV_PRICE_COLS column must FAIL, never write a partial asset
    # that still earns a valid manifest (the §二十-5 failure mode).  The account
    # part stays optional (ablation-only).
    missing_price = sorted(MARKET_ENV_PRICE_COLS - set(out.columns))
    if missing_price:
        raise ValueError(
            "build_market_env: market_env PRICE columns missing: "
            f"{missing_price} — the market_env required channel must carry the "
            f"full {sorted(MARKET_ENV_PRICE_COLS)} set.  Fix the upstream "
            "sources (highs_lows.parquet / industry_ranking.parquet / daily).")
```

3c. `contract.py`：在 `FUNDAMENTALS` 之后加 market_env DataContract（照 MARGIN 范式，7 列 = `MARKET_ENV_COLS`，日期在 index）：

```python
MARKET_ENV = DataContract(
    dataset_name="market_env_daily",
    primary_key=("date",),  # DatetimeIndex-backed broadcast file
    required_columns=(
        "high_low_ratio", "market_adv_ratio", "market_turnover_z",
        "mkt_cap_total_z", "avg_account_cap_z",
        "investor_new_num", "investor_new_z",
    ),
    units={},
    price_basis="n/a",
    timezone="Asia/Shanghai",
    calendar="SSE_SZSE",
    allowed_missingness={
        "mkt_cap_total_z": "account part absent before account_stats coverage",
        "avg_account_cap_z": "account part absent before account_stats coverage",
        "investor_new_num": "account part absent before account_stats coverage",
        "investor_new_z": "account part absent before account_stats coverage",
    },
)
```

并在 `CONTRACTS` 字典（:639-648）加 `MARKET_ENV.dataset_name: MARKET_ENV`。列名引用 `stoke_ml.features.aux_cols.MARKET_ENV_COLS` 可避免手抄漂移（先读 aux_cols 确认）。

3d. `broadcast_assets.py` MARKET_ENV_ASSET 加 `column_contract="market_env_daily"`（:34-40）：

```python
MARKET_ENV_ASSET: DataAssetContract = contract_for_channel(
    "market_env",
    data_type="market_env_daily",
    partition="single_file",
    extent_column="date",  # DatetimeIndex-backed
    effective_date_policy="index_date",
    column_contract="market_env_daily",
)
```

3e. `main()` 的空检查（:307）保留；PRICE 缺失由 `build_market_env` 的 ValueError 向上冒（main 不 catch → 进程退出非 0）。

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_build_market_env.py -q`
Expected: 全绿。既有测试若只喂 industry 上游（无 highs_lows/daily）现会因 PRICE 缺失而预期失败——把测试改为喂齐三源或改测失败路径（对齐 §v18-5 语义）。
再跑一次全量 scripts 测试确认无其他依赖旧上游路径：
Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_train_panel_formal_manifest.py tests/scripts/test_channel_manifest.py -q`

- [ ] **Step 5: Commit**

```bash
git add scripts/production/build_market_env.py stoke_ml/data/broadcast_assets.py stoke_ml/data/contract.py tests/scripts/test_build_market_env.py
git commit -m "fix(v18-T5): market_env formal build asserts full PRICE cols, market_adv_ratio reads industry_ranking.parquet, MARKET_ENV_ASSET column contract (§二十-5)"
```

**依赖**：无。**风险**：Med（`build_market_env` 从「partial OK」变「missing PRICE FAIL」——干净环境若缺 highs_lows/industry_ranking 会拒，这正是 audit 要的；既有测试需按新语义对齐）。

---

## 第二批：P1 — 工程治理（T6–T10）

### T6 — Streaming 磁盘检查按 scratch/panel 两个文件系统分别计算（§二十-6，Med）

**Files:**
- Modify: `scripts/production/train_panel_panel.py`（`_streaming_disk_required_gb` :1057-1069 拆 final/scratch、`_enforce_streaming_disk_space` :1116-1152 按 fs 检查）
- Modify: `stoke_ml/features/panel_builder.py`（兜底 :593-613 按 fs 检查）
- Test: `tests/scripts/test_train_panel_universe.py`（或既有 streaming disk 测试文件）

- [ ] **Step 1: 写失败测试**——在既有 streaming-disk 测试文件（若 test_train_panel_universe.py 无，则 `tests/scripts/` 下新建 `test_streaming_disk_per_fs.py`）加：

```python
def test_streaming_disk_check_separates_filesystems(tp, monkeypatch, tmp_path):
    """§v18-6: scratch and panel_store on DIFFERENT volumes must be sized
    independently — the panel-only volume must not be asked to also fit the
    scratch pickles."""
    import shutil as _shutil
    scratch = tmp_path / "scratch"
    panel = tmp_path / "panel"
    os.makedirs(scratch); os.makedirs(panel)
    args = _panel_args("revision-safe", panel_store=str(panel))
    # panel volume is tiny (fits panel only), scratch volume is huge.
    def fake_usage(p):
        if os.path.realpath(p) == os.path.realpath(str(scratch)):
            return _shutil.disk_usage  # not reached: st_dev distinguishes first
    ...
```

（实现者按既有 `_enforce_streaming_disk_space` 测试的 fixture 补齐——核心用 monkeypatch `os.stat` 让 scratch/panel 的 `st_dev` 不同、monkeypatch `shutil.disk_usage` 分别返回大/小 free，断言：panel 盘只被要求 fit `final_gb + margin`；旧实现把 `final+scratch+margin` 全算在 scratch 盘 → 假失败/假通过。）

- [ ] **Step 2: 实现**

2a. `train_panel_panel.py` `_streaming_disk_required_gb`（:1057-1069）改为返回 `(final_gb, scratch_gb)`：

```python
def _streaming_disk_required_gb(
    n_stocks: int, n_timesteps: int, n_features: int,
) -> tuple[float, float]:
    """§v18-6 disk required for a streaming build (GB), split by destination.

    Returns ``(final_panel_gb, scratch_gb)`` — the final grids land on the
    panel_store volume, the scratch pickles on the scratch volume.  A
    cross-filesystem build sizes each disk independently; a same-filesystem
    build sums them (the caller decides).  Never under-budgets.
    """
    final_panel = (
        int(n_stocks) * int(n_timesteps) * int(n_features) * 4 / (1024 ** 3)
    )
    scratch = (
        int(n_stocks) * int(n_timesteps) * int(n_features) * 8
        * _SCRATCH_PICKLE_OVERHEAD / (1024 ** 3)
    )
    return final_panel, scratch
```

2b. 新增 `_same_filesystem(a, b)` helper（放 `_enforce_streaming_disk_space` 前）：

```python
def _same_filesystem(a: str, b: str) -> bool:
    """True when two paths resolve to the same volume (``os.stat`` device id).

    Windows and POSIX both expose the volume/device via ``st_dev``.  An
    un-stat-able path (not yet created) degrades to True (same) so the
    conservative COMBINED check applies.
    """
    try:
        return os.stat(a).st_dev == os.stat(b).st_dev
    except OSError:
        return True
```

2c. `_enforce_streaming_disk_space`（:1116-1152）改为按 fs 检查：

```python
def _enforce_streaming_disk_space(
    args, stock_list: list[str], data_dir, scratch_dir: str,
) -> tuple[float, float] | None:
    """§v18-6 refuse to start a streaming build whose disk footprint cannot fit.

    Estimate-based pre-build check, sized PER FILESYSTEM: the final panel grids
    land on the ``--panel-store`` volume, the scratch pickles on the scratch
    volume.  When both resolve to the SAME volume the combined footprint is
    checked once; when they differ, each disk is checked for exactly what it
    must hold (+ safety margin).  No ``--panel-store`` (dense build) → no-op
    (None).  The builder's exact post-Pass-1 backstop is the second,
    un-underestimating net.
    """
    if not args.panel_store:
        return None
    est = _streaming_disk_estimate(args, stock_list, data_dir)
    if est is None:
        return None
    os.makedirs(scratch_dir, exist_ok=True)
    os.makedirs(args.panel_store, exist_ok=True)
    final_gb, scratch_gb = _streaming_disk_required_gb(*est)
    margin_gb = _scratch_safety_margin_gb()
    n_stocks, n_timesteps, n_features = est
    if _same_filesystem(scratch_dir, args.panel_store):
        need = final_gb + scratch_gb + margin_gb
        free = shutil.disk_usage(scratch_dir).free / (1024 ** 3)
        if need > free:
            raise SystemExit(
                f"streaming panel build into --panel-store requires "
                f"{need:.1f} GB which exceeds free space {free:.1f} GB on the "
                f"shared drive ({scratch_dir}) (n_stocks={n_stocks} x "
                f"n_timesteps={n_timesteps} x n_features={n_features} + "
                f"scratch pickles + margin) — the build cannot fit (§v18-6).  "
                f"Point --scratch-dir at a drive with more room, or shrink the "
                f"universe/stocks.")
        return need, free
    scratch_free = shutil.disk_usage(scratch_dir).free / (1024 ** 3)
    panel_free = shutil.disk_usage(args.panel_store).free / (1024 ** 3)
    problems = []
    if scratch_gb + margin_gb > scratch_free:
        problems.append(
            f"scratch pickles {scratch_gb:.1f} GB + margin {margin_gb:.1f} GB "
            f"exceeds free space {scratch_free:.1f} GB on the scratch drive "
            f"({scratch_dir})")
    if final_gb + margin_gb > panel_free:
        problems.append(
            f"final panel {final_gb:.1f} GB + margin {margin_gb:.1f} GB "
            f"exceeds free space {panel_free:.1f} GB on the panel_store drive "
            f"({args.panel_store})")
    if problems:
        raise SystemExit(
            "streaming panel build disk pre-check FAILED (§v18-6): "
            + "; ".join(problems)
            + f" (n_stocks={n_stocks} x n_timesteps={n_timesteps} x "
            f"n_features={n_features}) — free disk space or re-point "
            f"--scratch-dir / --panel-store to a larger drive.")
    return final_gb + scratch_gb + margin_gb, min(scratch_free, panel_free)
```

2d. `panel_builder.py` 兜底（:593-613）改按 fs（`memmap_dir` 是 build 实参，最终网格落它所在盘）：

```python
        if max_T and N_stocks:
            scratch_bytes = sum(
                os.path.getsize(os.path.join(scratch, f"{c}.pkl"))
                for c in valid_codes
            )
            grid_bytes = N_stocks * max_T * len(all_cols) * 4
            margin = _DEFAULT_SCRATCH_SAFETY_MARGIN_GB * (1024 ** 3)
            # §v18-6: per-filesystem — final grids on the memmap/panel_store
            # volume, scratch pickles on the scratch volume; combined when same.
            try:
                same_fs = os.stat(scratch).st_dev == os.stat(memmap_dir).st_dev
            except OSError:
                same_fs = True
            if same_fs:
                need = grid_bytes + scratch_bytes + margin
                free = shutil.disk_usage(scratch).free
                if need > free:
                    raise RuntimeError(...  # 复用既有 message 文案 ...)
            else:
                scratch_free = shutil.disk_usage(scratch).free
                panel_free = shutil.disk_usage(memmap_dir).free
                problems = []
                if scratch_bytes + margin > scratch_free:
                    problems.append(
                        f"scratch pickles {scratch_bytes/(1024**3):.1f} GB + "
                        f"margin {margin/(1024**3):.1f} GB exceeds free "
                        f"{scratch_free/(1024**3):.1f} GB on the scratch "
                        f"volume ({scratch})")
                if grid_bytes + margin > panel_free:
                    problems.append(
                        f"final grids {grid_bytes/(1024**3):.1f} GB + margin "
                        f"{margin/(1024**3):.1f} GB exceeds free "
                        f"{panel_free/(1024**3):.1f} GB on the panel volume "
                        f"({memmap_dir})")
                if problems:
                    raise RuntimeError(
                        "streaming panel build: exact disk footprint does not "
                        "fit (§v18-6): " + "; ".join(problems)
                        + f" (N_stocks={N_stocks} x max_T={max_T} x "
                        f"len(cols)={len(all_cols)})")
```

> 实现者确认 `_build_panel_streaming` 内 `memmap_dir` 的实际变量名（可能是 `memmap_dir` 或 `sink`），按实参对齐。

- [ ] **Step 3: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/ tests/features/test_panel_builder_scratch.py -q`
Expected: 全绿（含新 per-fs 测试 + 既有 streaming/scratch 测试不破）。

- [ ] **Step 4: Commit**

```bash
git add scripts/production/train_panel_panel.py stoke_ml/features/panel_builder.py tests/scripts/
git commit -m "fix(v18-T6): streaming disk pre-check + backstop sized per filesystem (scratch vs panel_store) (§二十-6)"
```

**依赖**：无。**风险**：Med（返回形状从单值变 tuple——调用方不消费返回值，仅测试可能断言，需对齐）。

---

### T7 — Derived Asset Manifest 加 upstream_roots + transform_code_hash + transform_config_hash（§二十-7，Med）

**Files:**
- Modify: `scripts/production/build_market_env.py`（`write_market_env` :272-289、`main` :292-326）
- Test: `tests/scripts/test_build_market_env.py`

- [ ] **Step 1: 写失败测试**

```python
def test_write_market_env_manifest_carries_lineage(tmp_path):
    """§v18-7: the derived market_env manifest records upstream_roots +
    transform_code_hash + transform_config_hash — freshness/derivation lineage,
    not just self-integrity."""
    daily = _write_industry(tmp_path)
    # feed highs_lows + daily so a full PRICE build succeeds
    ...  # 同 T5 的三源构造
    df, parts = bme.build_market_env(str(tmp_path))
    out = bme.write_market_env(str(tmp_path), df, parts)
    with open(out + ".manifest.json", encoding="utf-8") as f:
        m = json.load(f)
    assert "upstream_roots" in m and isinstance(m["upstream_roots"], dict)
    assert "daily" in m["upstream_roots"]
    assert m.get("transform_code_hash") and len(m["transform_code_hash"]) == 64
    assert "transform_config_hash" in m and m["transform_config_hash"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_build_market_env.py -k "lineage" -v`
Expected: FAIL（manifest 无 lineage 键）。

- [ ] **Step 3: 实现**

3a. 加 helper（`build_market_env.py` import 区加 `import hashlib`、`from stoke_ml.models.panel.code_tree_hash import hash_json`、`from scripts.production.data_quality_gate import dataset_fingerprint`）：

```python
def _file_sha256(path: str) -> str:
    try:
        h = hashlib.sha256()
        with open(path, "rb") as fh:
            for chunk in iter(lambda: fh.read(1 << 20), b""):
                h.update(chunk)
        return h.hexdigest()
    except OSError:
        return "<unreadable>"


def _upstream_roots(data_dir: str) -> dict:
    """The exact input assets this market_env build derives from (§v18-7)."""
    base = os.path.join(data_dir, "a_shares")
    return {
        "daily": dataset_fingerprint(data_dir, ["daily"]),
        "highs_lows": _file_sha256(
            os.path.join(base, "market_breadth", "highs_lows.parquet")),
        "industry_ranking": _file_sha256(
            os.path.join(base, "industry_ranking.parquet")),
        "account_stats": _file_sha256(
            os.path.join(base, "market_breadth", "account_stats.parquet")),
    }


def _transform_code_hash() -> str:
    """Content hash of THIS builder's source — the transform identity (§v18-7)."""
    return _file_sha256(os.path.abspath(__file__))
```

3b. `write_market_env` 签名加 `skip_turnover: bool = False`，manifest 写 `extra`：

```python
def write_market_env(
    data_dir: str, df: pd.DataFrame, parts: dict, *,
    skip_turnover: bool = False,
) -> str:
    """Atomically write the parquet + MARKET_ENV_ASSET manifest, then self-check.

    The manifest carries the channel-level vintage (``vintage_pit="proxy"``),
    the per-part ``parts`` declaration, AND the §v18-7 DERIVATION LINEAGE:
    ``upstream_roots`` (content hash of every input asset the build read),
    ``transform_code_hash`` (this builder's own source), and
    ``transform_config_hash`` (the build's config inputs).  A formal read
    (``require_valid_manifest=True``) of the re-read file must pass.
    """
    br = os.path.join(data_dir, "a_shares", "market_breadth")
    os.makedirs(br, exist_ok=True)
    out_path = os.path.join(br, "market_env_daily.parquet")
    with AtomicCommit(out_path) as ac:
        df.to_parquet(ac.tmp_path, compression="lz4")
    write_asset_manifest(
        out_path, MARKET_ENV_ASSET, df, parts=parts,
        upstream_roots=_upstream_roots(data_dir),
        transform_code_hash=_transform_code_hash(),
        transform_config_hash=hash_json({
            "skip_turnover": bool(skip_turnover),
            "output_columns": sorted(OUTPUT_COLUMNS),
            "parts": parts,
        }),
    )
    reread = pd.read_parquet(out_path)
    check_asset_read(out_path, MARKET_ENV_ASSET, reread,
                     require_valid_manifest=True)
    return out_path
```

3c. `main()` 传 `skip_turnover`：

```python
    out_path = write_market_env(
        data_dir, df, parts, skip_turnover=args.skip_turnover)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_build_market_env.py -q`
Expected: 全绿（含 lineage 断言 + 既有 round-trip/formal read 不破——`check_asset_read` 只校验 rows/schema_hash/extent/data_type 等既有键，新增键不干扰）。

- [ ] **Step 5: Commit**

```bash
git add scripts/production/build_market_env.py tests/scripts/test_build_market_env.py
git commit -m "feat(v18-T7): derived market_env manifest records upstream_roots + transform_code_hash + transform_config_hash (§二十-7)"
```

**依赖**：T5（build 成功路径已强制全 PRICE）。**风险**：Med（`dataset_fingerprint` 语义在 T8 会改成 manifest-root——两任务同批串行，T7 落盘的 upstream.daily 在 T8 后自然是新 fingerprint；`check_asset_read` 不校验未知键，round-trip 稳定）。

---

### T8 — Daily dataset fingerprint 改成 manifest-root Merkle 聚合（§二十-8，Med）

**Files:**
- Modify: `scripts/production/data_quality_gate.py`（`dataset_fingerprint` :319-375、`QUALITY_GATE_VERSION` :189 → "3.0"）
- Modify: `stoke_ml/data/asset_contract.py`（T2 已加 `manifest_body_digest`）
- Test: `tests/data/test_data_quality_gate.py`（:763-781 改带 manifest）

- [ ] **Step 1: 写失败测试**——更新 `test_dataset_fingerprint_deterministic_and_sensitive`（:763-774）让 daily 文件带 manifest（新语义：fingerprint 绑 manifest，数据变化必须伴随 manifest 重写）：

```python
    def test_dataset_fingerprint_deterministic_and_sensitive(self, tmp_path):
        root = tmp_path
        daily = root / "a_shares" / "daily"
        daily.mkdir(parents=True)
        self._write_daily_with_manifest(daily, "000001", TRADE_DATES, [10.0] * 5)
        h1 = dataset_fingerprint(root, ["daily"])
        assert dataset_fingerprint(root, ["daily"]) == h1
        # §v18-8: the fingerprint binds the per-stock MANIFEST (content hash),
        # not a byte re-scan.  Rebuilding the data MUST rewrite the manifest —
        # the canonical write path — and the digest flips.
        self._write_daily_with_manifest(
            daily, "000001", TRADE_DATES + ["2024-01-09"], [10.0] * 6)
        assert dataset_fingerprint(root, ["daily"]) != h1

    def test_dataset_fingerprint_missing_manifest_is_a_marker(self, tmp_path):
        """§v18-8: a parquet WITHOUT a manifest hashes a distinct marker — the
        fingerprint differs from the same parquet WITH a manifest (fail-closed:
        a manifest-less file is never silently treated as identical)."""
        root = tmp_path
        daily = root / "a_shares" / "daily"
        daily.mkdir(parents=True)
        _daily(TRADE_DATES, [10.0] * 5).to_parquet(daily / "000001.parquet", index=False)
        h_no_manifest = dataset_fingerprint(root, ["daily"])
        self._write_daily_with_manifest(daily, "000001", TRADE_DATES, [10.0] * 5)
        h_with_manifest = dataset_fingerprint(root, ["daily"])
        assert h_no_manifest != h_with_manifest

    @staticmethod
    def _write_daily_with_manifest(daily_dir, code, dates, closes):
        from stoke_ml.data.asset_contract import schema_hash
        df = _daily(dates, closes)
        df.to_parquet(daily_dir / f"{code}.parquet", index=False)
        with open(daily_dir / f"{code}.manifest.json", "w", encoding="utf-8") as f:
            json.dump({
                "code": code, "rows": len(df), "schema_hash": schema_hash(df),
                "source": "test", "start": dates[0], "end": dates[-1],
                "written_at": "2026-08-08T00:00:00Z",
            }, f)
```

> 若 `schema_hash`（asset_contract）与 daily store 的 `_schema_hash` 值语义不同，manifest 值不必等于 storage 的——fingerprint 只关心「manifest 内容是否随数据变化」；用 asset_contract.schema_hash 即可（同样随内容变化）。实现者按测试文件既有 `_daily` helper 对齐。

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/data/test_data_quality_gate.py -k "fingerprint" -v`
Expected: `test_dataset_fingerprint_deterministic_and_sensitive` FAIL（无 manifest 时重建不翻转）、`test_dataset_fingerprint_missing_manifest_is_a_marker` FAIL（无 manifest 与有 manifest 相同）。

- [ ] **Step 3: 实现**——重写 `dataset_fingerprint` 的 daily 分支（其余数据集保持字节流）：

```python
def dataset_fingerprint(root: Path, datasets: list[str]) -> str:
    """Deterministic content-aware hash over the required dataset directories.

    §v18-8: for the ``daily`` dataset the hash is a MERKLE ROOT over the
    per-stock manifest CONTENT hashes (``daily/{code}.manifest.json``, with
    ``written_at`` excluded) — the manifest is the data's identity, bound at
    write time.  A formal re-run reads only the small sidecars, never re-scans
    the full-market Daily bytes (tens of GB).  A parquet whose manifest was
    rewritten to reflect new content (the canonical write path) flips the
    digest; a manifest-less parquet hashes a distinct marker so it never
    silently equals a manifested one.

    Non-daily datasets (features / features_panel / custom) keep the legacy
    byte-streaming hash — they have no per-stock manifest contract to bind.
    Any dataset a training run consumes that changed after the gate PASS flips
    the digest and train_panel's report-match check fails (§六-2).
    """
    def _resolve(name: str) -> Path:
        if name == "daily":
            return Path(root) / "a_shares" / "daily"
        if name == "features":
            return Path(root) / "features"
        if name == "features_panel":
            return Path(root) / "features_panel"
        return Path(root) / name

    def _hash_file(fp: Path, h) -> None:
        try:
            with open(fp, "rb") as fh:
                while True:
                    chunk = fh.read(1 << 20)
                    if not chunk:
                        break
                    h.update(chunk)
        except OSError:
            h.update(fp.name.encode("utf-8"))
            h.update(b":unreadable;")

    h = hashlib.sha1()
    for ds in sorted(datasets):
        d = _resolve(ds)
        h.update(ds.encode("utf-8"))
        h.update(b"=")
        h.update(str(d.resolve()).encode("utf-8"))
        h.update(b"\x00")
        if not d.is_dir():
            h.update(b"missing")
            h.update(b";")
            continue
        if ds == "daily":
            # §v18-8: Merkle root over per-stock manifest content digests.
            for fp in sorted(d.glob("*.parquet")):
                code = fp.stem
                mp = d / f"{code}.manifest.json"
                digest = (
                    manifest_body_digest(str(mp))
                    if os.path.isfile(mp) else "<no-manifest>")
                h.update(fp.name.encode("utf-8"))
                h.update(b":")
                h.update(digest.encode("utf-8"))
                h.update(b";")
            continue
        for fp in sorted(d.glob("*.parquet")):
            h.update(fp.name.encode("utf-8"))
            h.update(b":")
            _hash_file(fp, h)
            h.update(b";")
    return h.hexdigest()[:16]
```

import 区加 `from stoke_ml.data.asset_contract import manifest_body_digest`。`QUALITY_GATE_VERSION`（:189）`"2.0"` → `"3.0"`（fingerprint 算法变了，旧 gate report 的 `data_manifest_hash` 不再可比——诚实拒绝，要求重跑 gate）。

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/data/test_data_quality_gate.py tests/scripts/test_train_panel_universe.py tests/models/panel/test_panel_store.py -q`
Expected: 全绿。既有 `test_dataset_fingerprint_missing_dir_is_stable`（:776-781）仍过（missing dir → "missing" marker，确定性保持）。若某测试断言 `QUALITY_GATE_VERSION == "2.0"`，同步改 "3.0"。

- [ ] **Step 5: Commit**

```bash
git add scripts/production/data_quality_gate.py tests/data/test_data_quality_gate.py
git commit -m "perf(v18-T8): daily dataset fingerprint is a Merkle root over per-stock manifest content hashes; bump gate version to 3.0 (§二十-8)"
```

**依赖**：T2 的 `manifest_body_digest`。**风险**：Med（fingerprint 算法变更 → 所有旧 gate report 的 data_manifest_hash 失配 → 正式训练要求重跑 gate；这是诚实的语义迁移，非 bug。`panel_store_meta` 的 `data_manifest_hash` 同函数，构建时自动用新算法）。

---

### T9 — Lockbox 文档声明：revision-safe headline_v1 含 industry proxy（§二十-9，Doc-only）

**Files:**
- Modify: `CONTEXT.md`（研究协议段落）
- Modify: `stoke_ml/config/feature_profile.py`（headline_v1 docstring 声明）

**决策**：audit §十一 建议最终 Lockbox 用 `headline_strict_v1` 或明确说明 revision-safe 仍含 industry proxy。**用户选择文档声明**（不新建 profile）。

- [ ] **Step 1: 加声明**——`feature_profile.py` 的 `FEATURE_PROFILES` 注释（:272-327）末尾追加：

```python
# §v18-9 (Lockbox declaration): ``headline_v1`` is the REVISION-SAFE formal
# baseline — it is NOT headline-strict.  ``industry`` remains required yet its
# historical sector classification is source_vintage=immutable_snapshot,
# transform=formula_versioned, pit_alignment=proxy (a reconstruction / proxy of
# today's sector map over history).  A formal headline_v1 conclusion therefore
# carries an industry PROXY in its feature set; the Lockbox final result may
# either adopt a future headline_strict_v1 (dropping industry) or keep
# headline_v1 and declare the proxy explicitly in the research writeup.
```

- [ ] **Step 2: CONTEXT.md 研究协议段加对应声明**（在 T4 加的段落附近）：

```markdown
### Lockbox 声明：revision-safe headline_v1 含 industry proxy（v18 §二十-9）

`headline_v1`（默认 formal profile）是 **revision-safe** 档，不是
headline-strict：其 required channel 里的 `industry` 历史行业分类带
`pit_alignment=proxy`（源：immutable_snapshot 行业快照 × formula_versioned
分类算法）。正式 headline_v1 结论的 feature set 内含该 industry proxy。
Lockbox 最终主结论二选一：(a) 新建 headline_strict_v1 剔除 industry；(b)
沿用 headline_v1 并在研究报告中显式声明该 proxy。当前按 (b) 声明，不建新
profile。
```

- [ ] **Step 3: 跑 docs 门禁**

Run: `PYTHONPATH=. ./.venv/Scripts/python scripts/production/check_docs_consistency.py`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git add CONTEXT.md stoke_ml/config/feature_profile.py
git commit -m "docs(v18-T9): declare revision-safe headline_v1 contains the industry proxy; no new profile (§二十-9)"
```

**依赖**：无。**风险**：Low。

---

### T10 — 剩余 production Run Manifest failure 从 warning 改成 non-zero exit（§二十-10，Low）

**Files:**
- Modify: `scripts/production/download_industry.py:87-96`
- Modify: `scripts/production/download_industry_ranking.py:159-167`
- Test: `tests/scripts/`（两个脚本的 run-manifest 失败 exit 测试；若无，在 `test_channel_manifest.py` 或新建）

- [ ] **Step 1: 写失败测试**（若已有 download 脚本测试文件则追加；否则新建 `tests/scripts/test_download_run_manifest_exit.py`）：

```python
import importlib.util, pathlib, pytest, sys

def _load_script(name):
    p = pathlib.Path(__file__).resolve().parents[2] / "scripts" / "production" / name
    spec = importlib.util.spec_from_file_location(name.replace(".py", ""), p)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod

def _monkey_main(mod, monkeypatch, tmp_path, fail_write):
    import argparse
    def _fake_parser(*a, **k):
        return argparse.Namespace(config=None, output=str(tmp_path),
                                  no_mapping=True, start="20240101", end="20241231")
    def _fake_write(*a, **k):
        if fail_write:
            raise OSError("simulated run-manifest write failure")
    monkeypatch.setattr(mod.argparse, "ArgumentParser",
                        lambda *a, **k: argparse.ArgumentParser(add_help=False))
    # route parse_args to a fixed namespace + write_run_manifest to the fake
    ...
```

（实现者按既有 download 脚本测试模式（如 `test_download_industry.py` 若有）补齐——核心断言：`write_run_manifest` 抛异常时脚本 `sys.exit(1)`，不再是 warning + exit 0。）

- [ ] **Step 2: 实现**

`download_industry.py:95-96` 改为：

```python
    except Exception as exc:
        # §v18-10: for Formal data production a successful data write with a
        # FAILED run manifest is a non-successful task — without the run
        # manifest the acquisition cannot be proven complete.  Exit non-zero.
        logger.error("run manifest write failed: %s", exc)
        sys.exit(1)
```

`download_industry_ranking.py:166-167` 同样改：

```python
    except Exception as exc:
        logger.error("run manifest write failed: %s", exc)
        sys.exit(1)
```

- [ ] **Step 3: 跑测试确认通过**

Run: `PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/scripts/test_download_run_manifest_exit.py -q`（或既有测试文件）
Expected: 全绿（normal path exit 0 + manifest 落盘；manifest 写失败 exit 1）。

- [ ] **Step 4: Commit**

```bash
git add scripts/production/download_industry.py scripts/production/download_industry_ranking.py tests/scripts/
git commit -m "fix(v18-T10): download run-manifest failure is a non-zero exit, not a warning (§二十-10)"
```

**依赖**：无。**风险**：Low。

---

## 明确不做的（推迟项）

- **`headline_strict_v1` profile**（audit §十一 建议项）：用户选择文档声明（T9），不新建 profile。
- **Formal 全部强制 prebuilt**（audit §九 建议项）：用户否决，维持 `>1000` 阈值（T4 只补文档）。
- **Live/Prebuilt 数学一致性再收紧**（audit §九 live vs processed 语义差异）：已由 T4 文档声明 + T2 manifest gate 兜底，不做进一步重构（超出本十项范围）。
- **新模型 / 继续加特征 / 模型层改动**：§十八 明确无红灯，本轮不做。
- **Double-read / TOCTOU 优化**（audit §十五）：audit 自评「若采用 Formal Prebuilt 主线，问题重要性下降」，且 T2 的 manifest gate 已前置验证——列为 backlog，不新增读写合并。

## 执行顺序

1. **第一批 P0（T1→T5 串行）**：T1（vintage 绑定）→ T2（consumed gate，依赖 T1）→ T3（era 复合，独立）→ T4（文档）→ T5（market_env，独立）。硬顺序仅 T1→T2。
2. **第二批 P1（T6→T10 串行）**：T6（磁盘）→ T7（lineage，依赖 T5 成功路径）→ T8（fingerprint，依赖 T2 的 manifest_body_digest）→ T9（文档）→ T10（run manifest exit）。T7 先于 T8 无硬依赖，但 T8 会改 `dataset_fingerprint` 语义——T7 的 `upstream_roots.daily` 在 T8 落地后自然用新算法，同一批内先 T7 后 T8 最稳。
3. 串行 subagent 流程天然无文件冲突。每项含专项测试。

## 端到端验证

每批完成后跑全仓测试确认无回归（v17 收尾同款三连）：
```bash
PYTHONPATH=. ./.venv/Scripts/python -m pytest tests/ -q                       # fast smoke
PYTHONPATH=. ./.venv/Scripts/python -m pytest -m "" tests/ -q                 # everything (~19min)
PYTHONPATH=. ./.venv/Scripts/python scripts/production/check_docs_consistency.py   # docs gate
```
每 Task 内专项验证见各 Task。十项全完后：
1. 全量测试 + docs 门禁 78/78（若 T4/T9 新增术语则门禁计数同步）。
2. **KB 沉淀**：把 v18 的 10 项 + 3 个 P0 结论写成 ADR 追加到 `C:\Users\16275\Desktop\PersonalKnowledgeBase\01-Projects\Stoke_MachineLearning.md` 的 `## 关键决策 (ADR)`（编号接 038 之后），含：
   - **ADR-039 (T1)**：named FeatureProfile 与其 VintagePolicy 构成不可拆分的研究配方——`--feature-profile X` 时 `--vintage-policy` 必须等于 profile 声明值，否则拒绝启动；ablation 通道进 required 才被 gate。
   - **ADR-040 (T2)**：Consumed ⊂ 语义——Formal Manifest Gate 与 aux_asset_root_hash 覆盖全部 consumed channel，required 只附加 coverage threshold；required⊆consumed 不变量。
   - **ADR-041 (T3)**：era_coverage 是复合契约（era_coverage AND era_observable_stock_fraction），分母是 requested universe，10/500 观察窗口不可代表全市场。
   - **ADR-042 (T6/T8)**：Streaming 磁盘按文件系统分别检查；Daily fingerprint 走 manifest-root Merkle，训练加载只读小 sidecar 不重扫 parquet。
   - 每份 ADR 一个独立 git commit（project name 在 message 里）。
3. 结束后按 audit §二十 结论：T1–T5 完成即**停止大规模架构修改**，进入 Baseline → Ablation → Continuous OOS → Lockbox 研究序列。
