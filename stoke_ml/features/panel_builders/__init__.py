"""Panel builder components extracted from panel_builder.py (refactor SS二十一).

Each module owns a single concern; ``panel_builder.build_panel_features``
orchestrates them.  The subpackage is private (leading-underscore modules);
external consumers import through ``stoke_ml.features.panel_builder``.
"""
